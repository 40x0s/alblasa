#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""الفصول 5–9: الهدف، الكسور الثلاثة، والرتق الدائم."""
from rtl_pdf import Doc  # noqa: F401  (للقراءة فقط)


# ═══════════════════════════════ 5 ═══════════════════════════════
def ch5(d):
    d.new_chapter(5, "الهدف: تشريح حماية ميزان Pro",
                  "ثلاث طبقات، دالة قرار واحدة، وأرقام مُتحقَّق منها")
    d.h1("٥.١  كيف يفكر من كتب الحماية")
    d.para("الفكرة في `README.md` واضحة: «الترخيص حالة، وليس شاشة». التطبيق لا يسأل المستخدم كل مرة، بل يقرأ **حالة مخزَّنة** ويصدقها. لذلك الطبقات الثلاث كلها **محلية**: لا خادم، لا توقيع رقمي، لا مفتاح خاص مخفي.")
    d.code_block("""[1] مفتاح الترخيص  →  TokenProcessor.ParseAndVerify(rawToken) : bool
      شكل: XXXXX-XXXXX-XXXXX-XXXXX من [A-Z0-9]
      القبول: (Σ ASCII % 73 == 0) && (XOR == 0x0A)
      ⚠ لا يرتبط بالجهاز إطلاقاً — أي مفتاح صالح يعمل على أي حاسوب

[2] الحالة المخزَّنة  →  ProductStateEngine.EvaluateCurrentState() : bool
      أ) HKCU\Software\MizanSolutions\Mizan\RuntimeState → ActivationBlob
         إن كان المفتاح المخزَّن صالحاً ← PRO (نعم، بدون أي تحقق إضافي)
         إن كان موجوداً لكنه غير صالح  ← يُحذَف (آلية «كشف التعديل اليدوي»)
      ب) %AppData%\MizanPro\state.enc  = Base64( AES-128-CBC( "MIZAN_VALID|<بصمة الجهاز>" ) )
         محتواه يجب أن **يحتوي** العلامة + البصمة ← يسمح بالتشغيل (ولا يرقّي إلى PRO)
      ج) InstallDate = yyyyMMdd ← 7 أيام تجربة، تُقرأ مرة واحدة عند الإقلاع

[3] الواجهة  →  AuthWindow / Controls/ActivationPanel / SettingsPage»الترخيص""",
                 title="المخطط الحقيقي كما هو في الكود",
                 note="المفتاح الثابت `Mizan2024_K3y128` ومتجه `MizanPro_IV_2024` مكتوبان نصاً في ProductStateEngine.cs (السطران 59 و62) — «تشفير» بمفتاح داخل الملف ليس تشفيراً من وجهة نظر أمنية، وهذا موضوع الفصل 12.")

    d.h1("٥.٢  الدالة التي نريدها — بكاملها")
    d.code_block("""public static bool ParseAndVerify(string rawToken)
{
    if (string.IsNullOrWhiteSpace(rawToken)) return false;
    string clean = rawToken.Replace("-", "").ToUpperInvariant();
    if (clean.Length != 20) return false;
    foreach (char c in clean)
        if (!((c >= 'A' && c <= 'Z') || (c >= '0' && c <= '9'))) return false;

    int  sum = clean.Sum(c => (int)c);
    byte xor = 0;
    foreach (char c in clean) xor ^= (byte)c;

    return sum % 73 == 0 && xor == RequiredXor;   // ← هنا نقف
}
private const byte RequiredXor = 0x0A;            // مضمَّن في IL (const)""",
                 title="Core/Engine/TokenProcessor.cs — السطور 62–94",
                 note="ملاحظة مهمة للدقة: `sum` و`xor` لا يعتمدان على أي شيء يخصّ الجهاز، ولا يوجد «ملف سرّي» ولا وقت صلاحية في المفتاح — لهذا الـ keygen ممكن (الفصل 10).")

    d.h1("٥.٣  الأهداف الثلاثة للرتق (اختر واحداً وافهم الباقي)")
    d.table(["ما تبحث عنه", "بايتات", "معنى الرتق", "النتيجة"],
            [["`ldc.i4.s 73` + `rem`", "1F 49 D0 → 1F **01** D0", "`sum % 1 == 0` دائماً صادق", "**أي مفتاح** بطول 20 وأبجدية صحيحة ومجموع… لا: أي مفتاح 20 محرفاً بصيغة صحيحة **مهما كان مجموعه**، والـ XOR ما زال مطلوباً"],
             ["`brfalse.s` بعد `rem`", "2C xx → **90 90**", "تخطّي فحص المجموع", "يبقى فحص XOR (0x0A) — نصف كسر"],
             ["كلافرعي القفز (`brfalse` + `bne.un`)", "2C xx → 90 90 و 39 xx → 90 90", "لا قفز إطلاقاً", "`return true` لكل مفتاح يمرّ بفحصي الطول والأبجدية — كسر كامل"],
             ["المقارنة مع 0x0A", "1F 0A → 1F **00**", "القارنة مع 0", "يغير «الذوق» فقط — لن نستخدمه: نمط `1F 0A` قد يتكرر"]],
            widths=[36, 30, 34, 74], align=["L", "L", "L", "L"])
    d.callout("warn", "لماذا اخترنا السطر الأول",
              ["`1F 49 D0` **نادر في الملف** (الثابت 73 نادر)، والتغيير **بحجم متطابق** (بايت واحد لا يتغير معه أي طول/إزاحة في جدول الـ metadata)، والشرط يصبح صادقاً **رياضياً** لا بقفزة تحكم — أي أنه أوضح تعليمياً.",
               "تحذير الدقة: بعد الرتق الأول يبقى فحص `xor == 0x0A` معمولاً به — لذلك سنستخدم في العرض مفتاحاً **طوله صحيح، أبجديته صحيحة، XOR صحيح، ومجموعه لا يقبل القسمة على 73**. هذا هو «المفتاح المُثبِت للرتق»."])

    d.h1("٥.٤  الأرقام التي سنقولهاعلى المسرح (كلها مُتحقَّق منها)")
    d.table(["المفتاح", "المجموع Σ", "% 73", "XOR", "قبل الرتق", "بعد رتق `73→1`"],
            [["M1Z4N-2025P-ROM4S-T3R01", "1314", "0 ✓", "0x0A ✓", "مقبول ✓", "مقبول"],
             ["AAAAABBBBBCCCCCADN89", "1314", "0 ✓", "0x0A ✓", "مقبول ✓", "مقبول"],
             ["5NOFK-JQB1Z-7HSHF-NOP6D", "1426", "39 ✗", "0x0A ✓", "**مرفوض**", "**مقبول** ← الدليل"],
             ["MIZAN-2026X-32DBG-B36SQ", "1314", "0 ✓", "0x0A ✓", "مقبول ✓", "مقبول"],
             ["26F30-20C2B-22E4B-B (بصمة جهاز!)", "—", "—", "—", "مرفوض (الطول 16)", "مرفوض"]]
            , widths=[52, 18, 16, 20, 26, 42], align=["L", "R", "R", "R", "L", "L"])
    d.bullets([
        "`1426 = 73 × 19 + 39` — لا يقبل القسمة ⇒ مرفوض؛ ومع أن `XOR = 0x0A` صحيح ⇒ الفشل من المجموع وحده. **لهذا المفتاح واحد مثالي لتجربة الرتق**.",
        "الصف الأخير مهم: في لقطة `the problem/215449.png` لُصقت بصمة الجهاز في حقل المفتاح، فأعاد المنسّق تقسيمها إلى `26F30-20C2B-22E4B-B` (5-5-5-1) ⇒ رسالة «مفتاح غير صالح» صحيحة لكنها **مضلِّلة للعين** — سنشرحها كدرس UX/أمان (الفصل 11).",
        "كل المفاتيح في الجدول وُلِّدت/فُحصت بسكربت الفصل 10 نفسه — لا يوجد رقم «تقريبي» في هذا الدليل."])


# ═══════════════════════════════ 6 ═══════════════════════════════
def ch6(d):
    d.new_chapter(6, "الكسر №1 — التزوير عند التخزين",
                  "بدون منقِّح، بدون رتق: اجعل الطبقة الثانية تصدّقك")
    d.para("هذه أسرع كسرة في المشروع، وأقربها إلى ما يفعله العالم الحقيقي حين يبيع «ترخيصاً مدى الحياة مسرَّباً»: لا نلمس البرنامج إطلاقاً — نكتب ما يتوقع البرنامج قراءته.")
    d.h1("٦.١  الكسرة (أ): `ActivationBlob` = مفتاح صالح")
    d.steps([
        "أغلق التطبيق، وشغّل `reset-license.bat` (الفصل ٣.٤).",
        "`reg add \"HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState\" /v ActivationBlob /t REG_SZ /d M1Z4N-2025P-ROM4S-T3R01 /f`",
        "شغّل `MizanPro.exe` → سجّل الدخول بـ `admin` / `Admin@123`.",
        "النتيجة المتوقعة: لا نافذة «تفعيل»، اللافتة تقول **PRO**، وصفحة الإعدادات»الترخيص تعرض المفتاح نفسه.",
        "**الدليل الذي يُقنع**: احذف القيمة وكرر — ستعود لافتة التجربة. الفرق الوحيد هو قيمة في الـ Registry."])
    d.callout("danger", "لماذا ينجح هذا؟ (الجواب الذي يسأل عنه المشرف)",
              ["`EvaluateCurrentState` يقرأ القيمة ثم يمرّرها إلى `ParseAndVerify` — أي أن **معيار الثقة هو صحة الصيغة الحسابية وحدها**، لا توقيع ولا ارتباط بالجهاز.",
               "البرنامج «يحمي» بمفتاح يمكن لأي أحد حساب صفاته، ثم يخزّن النتيجة في مكان **يكتبه المستخدم نفسه**. هذا ليس خللاً في `regedit` — هذا تصميم: لا يوجد **سبب** يجعل القيمة المخزَّنة غير قابلة للتزوير.",
               "الإصلاح (الفصل ١٢): اجعل القيمة المخزَّنة **نتيجة توقيع** بمفتاح لا يملكه المستخدم (أو اجعل القرار عند الخادم)."])

    d.h1("٦.٢  الكسرة (ب): تزوير `state.enc` (ونحن لا نملك المفتاح!)")
    d.para("هنا الجملة التعليمية: «ملف مشفّر بمفتاح AES» يبدو منيعاً — إلا أن المفتاح **داخل الملف نفسه** و`static readonly`. سنعيد إنتاج الـ ciphertext بحرية، والبرنامج سيقبله كمسار احتياطي صالح.")
    d.code_block("""# forge_state.py — يحتاج: pip install pycryptodome
# الاستخدام:  python forge_state.py <البصمة_التي_يعرضها_التطبيق>
import base64, os, sys
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad

KEY = b"Mizan2024_K3y128"          # ProductStateEngine.cs:59
IV  = b"MizanPro_IV_2024"           # ProductStateEngine.cs:62
fp  = sys.argv[1].strip().upper()  # مثل: 1A2B-3C4D-5E6F-7A8B  (انسخه من شاشة التفعيل)
plain = f"MIZAN_VALID|{fp}".encode()
ct = AES.new(KEY, AES.CBC, IV).encrypt(pad(plain, 16))
path = os.path.expandvars(r"%APPDATA%\\MizanPro\\state.enc")
os.makedirs(os.path.dirname(path), exist_ok=True)
open(path, "w").write(base64.b64encode(ct).decode())
print("written:", path)""",
                 title="C:\\Lab\\forge_state.py",
                 note="لا تحسب البصمة بنفسك — `GetFirstMacAddress` يعتمد WMI وترتيب المحوّلات، والاختلاف حرف واحد يُبطل الملف. **انسخها من الشاشة** (`معرّف الجهاز`) أو من صفحة الإعدادات.")
    d.bullets([
        "جرّبها مع `InstallDate` قديماً (الحالة (ب) في ٣.٤) → التطبيق كان سيرفض («انتهت التجربة») ← بعد كتابة `state.enc` **يعمل** رغم انتهاء التجربة: المسار الاحتياطي يسبق فحص التاريخ.",
        "لاحظ أنه **لا** يرقّي إلى PRO (المسار «صامت») — لا تبالغ في النتيجة أمام الجمهور؛ قل بالضبط ما يفعله.",
        "هذا مثال ممتاز لـ«الحماية نصف مكتملة»: المطوّر احتاط لفقدان الـ Registry، فنسف فعالية الحماية كلها."])
    d.h1("٦.٣  الكسرة (ج): إعادة ضبط التجربة (كلاسيكية الـ trial reset)")
    d.code_block("""reg add "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate /t REG_SZ /d %date:~10,4%%date:~4,2%%date:~7,2% /f""",
                 title="cmd · «ركّب اليوم» — سبعة أيام جديدة",
                 note="⚠ لا تُدخل هذا في العرض كأساس للكسر: `TrialPeriodDays` تُقرأ مرة واحدة عند الإقلاع ولا تتحدث أثناء الجلسة، ولأن `EvaluateCurrentState` يعمل داخل نافذةSplash قبل أن تتمكن من إيقاف المنقِّح — سنستخدمه فقط «تمهيداً» للفصل 8.")

    d.h1("٦.٤  ما الذي يثبت أن التطبيق «لم يكتب شيئاً» عند المفتاح الخاطئ")
    d.para("سؤال يطرحه الطلاب دائماً: «كيف نعرف أن الفشل فشل حقيقي؟». نفحص بـ Procmon أو بـ x32dbg log-only:")
    d.code_block("""$ bp advapi32.RegSetValueExW            :: نقطة توقف + Log text = {p:arg2}
   ثم أدخل مفتاحاً خاطئاً → لا توقف ⇒ لم تُكتب أي قيمة
   ثم أدخل مفتاحاً صحيحاً → يتوقف عند الاسم ActivationBlob ⇒ كُتبت
   (إن لم تظهر الرسالة: كليك يمين على الـ breakpoint → Break = Never + Command = gc)""",
                 title="إثبات سلوكي — لا رأي فيه",
                 note="البديل بلا أدوات: `reg query \"HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState\" /v ActivationBlob` قبل وبعد — إن ظهر `error: the system cannot find the file specified` ثم بعد التفعيل ظهرت القيمة، فهذا دليل مكتمل.")


# ═══════════════════════════════ 7 ═══════════════════════════════
def ch7(d):
    d.new_chapter(7, "الكسر №2 — رتق IL داخل الذاكرة بـ x32dbg",
                  "نجم العرض: 12 خطوة، بايت واحد، ودرس JIT الذي لا يُنسى")
    d.callout("note", "لماذا هذا الكسر هو «الأصغر والأنظف»؟",
              ["لا يغيّر أي ملف (فلا Anti-Virus، ولا فحص تكامل، ولا حاجة لإعادة تنشيط)، ولا يحتاج حتى مفتاحاً «مشتقاً» — بل يجعل **دالة التحقق نفسها** عاجزة عن الرفض. هذا هو تعريف patch في أدبيات الكراك: «تعديل القرار، لا الإدخال»."])
    d.h1("٧.١  القاعدة الزمنية (اقرأها مرتين)")
    d.para("`ParseAndVerify` لا تحتوي كود آلة حتى تُستدعى أول مرة. بعدها، تعديل صورة IL في الذاكرة **لا معنى له**: الكود المُنفَّذ هو نسخة مُترجَمة قديمة. لهذا الترتيب هو كل شيء: **رتق قبل أول محاولة تفعيل**.")
    d.table(["التسلسل", "ما يحدث", "النتيجة عند إدخال `5NOFK-JQB1Z-7HSHF-NOP6D`"],
            [["تشغيل ← ظهور النافذة ← **F12** ← رتق ← ثم محاولة التفعيل", "IL رُتق قبل أن يُترجم", "**مقبول** ✓ (هذا ما سنعرضه)"],
             ["تشغيل ← إدخال المفتاح (فشل) ← **F12** ← رتق ← إعادة المحاولة", "الطريقة تُرجمت من IL الأصلية أولاً", "**مرفوض** ✗ — وسنُظهره عمداً"],
             ["رتق على القرص (HxD) ثم تشغيل", "الحزمة كلها جديدة، لا ذاكرة قديمة", "**مقبول** (الفصل 9)"]]
            , widths=[62, 42, 70], align=["L", "L", "L"])
    d.h1("٧.٢  الخطوات — حرفياً كما سنضغطها")
    d.steps([
        "**تأكد من الحزمة الصحيحة**: `dir C:\\Lab\\MizanPro\\MizanPro.dll` يجب أن يوجد. إن لم يوجد فإعداد النشر لم يتغيّر — أوقف كل شيء وعد للفصل ٢.٣.",
        "افتح `x32dbg.exe` (مسار 32-بت) من `C:\\Lab\\x32dbg\\Release\\`، واضبط متغيرات البيئة في نفس نافذة cmd التي ستُطلق منها إن أردت `DOTNET_TieredCompilation=0`.",
        "`File → Open (F3)` واختر **`MizanPro.exe`** — لا `MizanPro.dll`؛ هذه هي الطريقة الوحيدة لتشغيل apphost تحت المنقِّح.",
        "**F9** — تجاوز System Breakpoint. تابع بـ F9 حتى تظهر نافذة تسجيل الدخول (قد تحتاج F9 مرة أو مرتين حسب `Entry point`).",
        "**F12** (إيقاف مؤقت). لا تخف من أن RIP في `ntdll` — لا نريد كود الآلة أصلاً.",
        "**Alt+M** لفتح خريطة الذاكرة، وابحث عن سطر الـ module باسم `MizanPro.dll` (ستجده قرب الأعلى بعد `MizanPro.exe`).",
        "علّك على ذلك السطر ← كليك يمين ← `Follow → In Memory Map`/`Dump`؛ أو ببلا: **Ctrl+B** (Binary search) ضمن النطاق «All modules»/`MizanPro.dll`.",
        "في نافذة البحث اكتب النمط: `1F 49 D0` ← `Find all`. **توقّع نتيجة واحدة**. إن ظهرت أكثر: ابحث عن `1F 49 D0 2C`، والنتيجة الصحيحة هي التي تليها بايت القفز `2C` وتقع داخل منطقة `.text` للـ DLL المُدار.",
        "انقر النتيجة ← **Ctrl+E** (تعديل الذاكرة الثنائية) ← غيّر البت `49` إلى `01` ← OK. (البايتان الآخران `1F` و`D0` كما هما.)",
        "**Ctrl+P** (Patches): تحقق أن السطر يقول `1F 01 D0` — هذه لقطة ممتاز للعرض: «رتق واحد، بحجمه الأصلي».",
        "**F9** للمتابعة. الآن ادخل `admin`/`Admin@123` ثم افتح «تفعيل المنتج» والصق: `5NOFK-JQB1Z-7HSHF-NOP6D` → **تم التفعيل!**",
        "**Klik يمين في الـ Disassembly → لا تفصل المنقِّح** — اترك العملية تعمل، ثم بعد العرض: أغلقها من x32dbg، وشغّل `reset-license.bat`، وتحقق أن `state.enc` و`ActivationBlob` حُذفا. الملف على القرص لم يُمسّ ⇒ أي فحص تكامل لن يشتكي."],
        size=9.9)
    d.callout("ok", "الدليل الكامل أمام الجمهور (ثلاث لقطات)",
              ["(١) قبل الرتق: نفس المفتاح ⇒ «مفتاح الترخيص غير صالح» (نسخة الـ Log/الرسالة).",
               "(٢) بعد الرتق: نفس المفتاح **بالضبط** ⇒ «تم التفعيل بنجاح» + اللافتة PRO + `reg query` يُظهر `ActivationBlob`.",
               "(٣) أعد تشغيل التطبيق بدون المنقِّح ⇒ المفتاح صار **غير صالح** مرة أخرى (الرتق كان في الذاكرة فقط) — هذه أقوى جملة تقنع المشرف أنك تفهم الفرق بين الذاكرة والملف."])
    d.h1("٧.٣  «التجربة الثانية»: لماذا لم ينجح الرتق هذه المرة؟")
    d.para("أعد كل شيء، لكن أدخل المفتاح **قبل** الـ patch (لتحصل على رسالة الرفض)، ثم رتّق، ثم أعد المحاولة ⇒ **سيُرفض**. اسأل الحضور: «ما الذي تغيّر؟» ثم أجب: الطريقة تُرجمت بواسطة JIT إلى كود آلة في الذاكرة، وصار تنفيذها لا يقرأ IL. ثم واصل: «لهذا نرتق على مستوى IL **قبل** الاستخدام الأول — أو نرتق الملف (الفصل ٩)». هذه اللحظة وحدها تساوي نصف الدرجة.")
    d.callout("warn", "خطة بديلة إن لم تجد النمط (احفظها)",
              ["(أ) تأكد أنك في `MizanPro.dll` لا `MizanPro.exe` (الأخير حزمة apphost).",
               "(ب) تأكد من إعداد النشر (`PublishSingleFile=false`) — بدونه لا يوجد `MizanPro.dll` على الإطلاق.",
               "(ج) افتح `MizanPro.dll` في **HxD** وابحث عن `1F 49 D0` على القرص؛ إن وجدت هناك ولا تجد في الذاكرة ⇒ أنت في نافذة DLL خطأ في خريطة الذاكرة (هناك `MizanPro.ni.dll` أو مناطق `Image`/`Heap`).",
               "(د) إن كان كل شيء صحيحاً والنمط مفقود: استخدم `1F 49 D0 2C`، أو ابحث عن `D0 2C` (rem + brfalse) قرب `1F 0A` (الحمل الثابت 10) — واختر الأول.",
               "(هـ) آخر خطة: ألغِ R2R (المتغير `DOTNET_ReadyToRun=0`) وأعد بناء الحزمة بدون `crossgen` — وراجع أنك لم تبنِ بملف `MizanPro.dll` من `dist` قديم."])
    d.h1("٧.٤  النسخة «الأكثر جرأة» (للقوة فقط، لا للضرورة)")
    d.para("بدل تغيير `73` إلى `1`، يمكن تخطي فرعي القفز معاً: `2C xx → 90 90` و`39 xx → 90 90` ⇒ الدالة تعود `true` لكل مفتاح **له 20 محرفاً من `A-Z0-9`** (فحوص الطول/الأبجدية تسبق القفزات، فما زالت تعمل). مفيد لإظهار أن «الترقيع ليس كلاسيكياً بايت واحد بالضرورة — أحياناً تعليمات قفز»؛ وأيضاً يُبقي احتمال أن يسألك أحد «وماذا لو كان المفتاح 21 محرفاً؟».")


# ═══════════════════════════════ 8 ═══════════════════════════════
def ch8(d):
    d.new_chapter(8, "الكسر №3 — API ورسائل ومنادٍ: تمرين فهم (ليس كسراً)",
                  "MessageBoxW · Call stack · EAX — وما الذي يبرره أكاديمياً")
    d.para("هذا هو الجزء الذي يجب أن نُسمّيه باسمه الصحيح أمام المشرف: **تشريح سلوك**، لا كسر. سنُري الطلاب كيف يرى المنقِّح نداءات Win32، وكيف تُقرأ قيمة الإرجاع، ثم نشرح بصراحة لماذا **لا** يغيّر ذلك شيئاً في MizanPro — وهذا الصدق هو ما يفرّق عرضاً هندسياً عن «خدعة».")
    d.h1("٨.١  جهّز الحالة: «انتهت التجربة»")
    d.code_block("""reg add "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate /t REG_SZ /d 20250101 /f
del  /f /q "%APPDATA%\\MizanPro\\state.enc"
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob /f""",
                 title="cmd · إنهاء التجربة (سطر واحد يكفي)",
                 note="بعد هذا، أول ما يظهر عند الإقلاع صندوق «انتهت فترة التجربة…». هنا نريد أن نكون تحت المنقِّح **قبل** أن يظهر.")
    d.h1("٨.٢  الخطوات")
    d.steps([
        "شغّل `MizanPro.exe` تحت x32dbg (F3) ثم **F9** حتى System Breakpoint.",
        "في شريط الأوامر: `bp user32.MessageBoxW` ثم **F9**. (`user32.dll` محمّل أصلاً في تطبيقات WPF؛ إن قال `module not found` تابع F9 حتى يظهر في Log ثم أعد الأمر. وقاية إضافية: `bp user32.MessageBoxIndirectW`.)",
        "**F9** — سيتوقف عند أول صندوق رسالة. في نافذة المكدّس (**Alt+K**) ستقرأ الإطارات من أعلى لأسفل: `user32.dll!MessageBoxW` ← `coreclr.dll` ← `clrjit.dll` ← … ← `MizanPro.dll`. **هنا تشرح للطلاب كيف يبدو .NET من الأسفل**: لا أسماء دوال C#، ولا رموز، ومكتبة واحدة للنواة وواحدة للمترجم.",
        "في لوحة المعطيات، انظر وسائط `MessageBoxW`: `hWnd`, `lpText`, `lpCaption`, `uType`. الأنيق أكثر: اجعل النقطة **Log-only** (كليك يمين → Log text = `{p:arg1}`، Break = Never، Command = `gc`) ⇒ نص الرسائل يصبّ في Log بلا توقف — «تحليل سلوكي» حي في 20 ثانية.",
        "**Ctrl+F9** (Execute till return) ⇒ تعود إلى ما بعد النداء، والقيمة في `EAX` (1 = IDOK، 2 = IDCANCEL، 6 = IDYES…).",
        "كليك يمين على `EAX` → `Modify` (أو في شريط الأوامر: `set eax, 0`) ثم **F9** وانقر «OK» في الصندوق.",
        "**النتيجة**: التطبيق يتصرف كما لو لم يتغيّر شيء. اسأل: «ليش؟» — لأن `App.xaml.cs` يستدعي `MessageBox.Show(...)` ثم `args.Handled = true` **بدون أن ينظر إلى النتيجة**: القرار اتُّخذ قبل الصندوق، والصندوق مجرد عرض."])
    d.callout("ok", "الجملة التي يجب أن تُقال حرفياً",
              ["«تعديل قيمة الإرجاع في حالة `MessageBoxW` يُغيّر **ردّ المستخدم**، لا **قرار البرنامج**. الكسر الحقيقي يغيّر القرار: إما الحساب الذي ينتجه (`% 73`)، أو مصدر الثقة (الـ Registry/`state.enc`). لهذا الفصل ٧ هو الكسر، وهذا الفصل هو التشريح».",
               "ثُمَّ أضف: «في برنامجٍ يستخدم نتيجة الصندوق ليقرر (`if (msg == IDYES) RegisterNow();`) كان تعديل `EAX` كسراً فعلياً — هذا هو الفرق بين أن تعرف الأوامر وأن تعرف أين القرار»."])
    d.h1("٨.٣  متغيرات مفيدة للعرض (إن بقي وقت)")
    d.bullets([
        "`bp MizanPro.dll:TokenProcessor.ParseAndVerify` — **لا تعمل**: x64dbg لا يعرف رموز .NET المُدارة. لهذا نرتق IL (بحث بايتات) بدل نقطة توقف بالاسم — قلها، فهي إجابة سؤال «ليش ما حطيت breakpoint على الدالة؟».",
        "`bpm <address>` (نقطة توقف على كتابة/قراءة ذاكرة): مفيدة في native، وخادعة هنا لأن عناوين المتغيرات المُدارة تتحرك مع الـ GC.",
        "لو أردت «كسراً» بهذا الأسلوب في ميزان Pro فعلاً: رتق `MessageBox.Show` إلى لا شيء (NOP عند أول بايت) = إزالة الـ nag فقط — وهو بالضبط اسمها في الأدبيات: `nag screen removal`.",
        "خيار «تجاهل الاستثناءات» عند `0xE0434352` يغيّر كمّ التوقفات كثيراً أثناء هذه الخطوات — لا تنسَ ضبطه من الفصل ٣.٣."])


# ═══════════════════════════════ 9 ═══════════════════════════════
def ch9(d):
    d.new_chapter(9, "الرتق الدائم: على القرص — ثلاث طرق بترتيب الاعتمادية",
                  "HxD ← dnSpyEx ← (خطة رابعة ذكية: لا تعدّل شيئاً)")
    d.h1("٩.١  الطريقة A — HxD على `MizanPro.dll` (الأكثر موثوقية، وأسهل شرحاً)")
    d.steps([
        "انسخ `C:\\Lab\\MizanPro\\MizanPro.dll` إلى `backup\\MizanPro.dll.orig`، واعمل على نسخة: `copy MizanPro.dll MizanPro_cracked.dll`.",
        "افتح النسخة في HxD ← `Edit → Search (Ctrl+F)` ← اختر **Hex values** ← اكتب `1F 49 D0` ← OK.",
        "ستجد موضعاً (أو أكثر) داخل القسم `.text`. غيّر البت الأوسط: `49` ← `01`.",
        "احفظ (Ctrl+S). تأكد أن **الحجم لم يتغيّر** وهذا سبب نجاح الطريقة: لا جديد في جدول الـ metadata، ولا إعادة حساب لأطول أو RVA — فقط محتوى جسم طريقة.",
        "ضع الملف المعدّل مكان الأصلي (أو في مجلد `cracked\\`) وشغّل `MizanPro.exe` **بدون** المنقِّح.",
        "نظّف الحالة أولاً (`reset-license.bat`) ثم أدخل المفتاح المثبت `5NOFK-JQB1Z-7HSHF-NOP6D` ⇒ **تم التفعيل**. الآن الأثر دائم: أعد التشغيل مرتين، احذف ما شئت من `HKCU` — سيكتب التطبيق حالته من جديد.",
        "أعد الملف الأصلي من `backup\\` بعد العرض (وانسخه أيضاً إلى مجلد التسليم)."])
    d.callout("warn", "نقطتان سيصنعان فرقاً في مناقشتك",
              ["(١) لا يوجد **Strong Name** ولا توقيع في مشروعنا — لو كان موجوداً لرفض CLR تحميل الحزمة بعد التعديل، ولسقط التطبيق عند الإقلاع. قل: «المشروع الحالي غير موقّع؛ إضافة توقيع + فحص تكامل هي أول توصية في الفصل ١٢»، وأنت بذلك تجيب قبل أن يُسأل.",
               "(٢) `DebugType=none` يعني لا PDB — لهذا لا يستطيع x32dbg أن يعرض لك أسماء المُنفَّذات الأصلية. لا تعتذر عن هذا: قل إنه **مقصود** لتقريب الهدف من برنامج حقيقي."])
    d.h1("٩.٢  الطريقة B — dnSpyEx: أوضح تعليمياً، وأدقّ تقنياً")
    d.code_block("""# dnSpyEx (مجاناً، مفتوح المصدر) — خطوات العرض
1) File → Open → C:\\Lab\\MizanPro\\MizanPro.dll      ← وليس exe!
2) الشجرة: MizanPro → MizanPro.Core.Engine → TokenProcessor → ParseAndVerify(string)
3) C# tab  : سترى الكود كاملاً بما فيه تعليقات الشرح — لحظة «واو» للطلاب.
4) IL tab  : «Edit IL Instructions…» → استبدل جسم الطريقة كله بـ:
        ldc.i4.1
        ret
     (بايتان فقط: 17 2A) ← أي مفتاح بطول 20 وأبجدية صحيحة سيمرّ
5) File → Save Module → MizanPro_cracked.dll (تأكد من «Don't use strong name» / Suppress PKT إن ظهر)
6) بدّل الملف في dist\\ وشغّل.""",
                 title="لماذا «جسم IL» وليس «محرر C#»؟",
                 note="محرر C# في dnSpy يعيد ترجمة الكود بـ Roslyn ويحتاج كل المرجعيات (System.Runtime, WindowsBase…). في نشر self-contained بدون ملف واحد تكون كل المكتبات **بجانب** الـ DLL ⇒ يعمل غالباً؛ لكن إن ظهر لك «could not load assembly» فاستخدم «Edit IL» كما فعلنا. لا تقدّم عرضاً يعتمد على شيء جرّبته مرة واحدة فقط.")
    d.h1("٩.٣  الطريقة C — «الكسر النظيف»: keygen بلا تعديل")
    d.para("لسنا ملزمين بتغيير برنامجنا لنثبت نقطة؛ أحياناً أوضح درس هو أن **الحماية نفسها قابلة للاشتقاق** (الفصل ١٠). الفرق بين الثلاثة يُشرح دائماً بنفس الجملة:")
    d.table(["الأسلوب", "ماذا يغيّر", "يدوم؟", "يكتشفه فحص التكامل؟", "قيمته التعليمية"],
            [["memory IL patch", "صورة DLL في الذاكرة", "لا (للجلسة)", "لا شيء يقيس", "يفسر JIT + متى تُرجم الطريقة"],
             ["file patch (HxD/dnSpy)", "ملف الحزمة", "نعم", "**نعم**", "يفسّر لماذا نوقّع/نفحص"],
             ["keygen", "لا شيء — نُرضي الشرط", "نعم", "لا يوجد ما يفحص", "يفسّر لماذا الشرطان الضعيفان قاتلان"]],
            widths=[34, 34, 24, 32, 49], align=["L", "L", "R", "L", "L"])
    d.h1("٩.٤  كيف تتأكد أن ما تراه «كسر» وليس «صدفة»")
    d.bullets([
        "جرّب مفتاحاً **مرفوضاً في الأصل** (مثل `5NOFK-…` أو `AAAAA-BBBBB-CCCCC-DDDDD`) — إن قُبل بعد الرتق فهذا دليل سببي، لا مجرد «اشتغل».",
        "جرّب مفتاحاً بطول خاطئ (`12345`) — يجب أن **يُرفض** بعد كل الرتقات (لأن فحص الطول يسبق)؛ هذا يُظهر أنك لم تعطّل الدالة كلها بل شرطاً واحداً.",
        "بعد الطريقة A/B: احذف `ActivationBlob` و`state.enc` ثم أعد التشغيل ⇒ يجب أن يعود إلى «تجربة» (الحالة لا تُخزَّن في الحزمة) — يثبت أن الكسر يغيّر **القرار** لا **التخزين**.",
        "وثّق بلقطة: نافذة `Ctrl+P` في x32dbg تُظهر الرتق المؤقت، وصورة الـ IL قبل/بعد من dnSpy — تكفيان لإقناع أي محكّم."])
