#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
build_guide_pdf.py — يولّد «دليل الكسر الكامل باستخدام x32dbg — من الصفر إلى المية».

    python3 docs-builder/build_guide_pdf.py            ← يكتب ../Crack_Guide_x32dbg.pdf

المحرّك العربي في rtl_pdf.py. كل النصوص هنا حرفياً هي ما سيُطبع؛ لا يوجد قالب آخر.
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from rtl_pdf import Doc, NAVY, GOLD, GREEN, RED, ORANGE, BLUE, GRAY, INK   # noqa: E402

OUT = os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "Crack_Guide_x32dbg.pdf")


# ═══════════════════════════════ الغلاف + الخريطة ═══════════════════════════════
def cover_and_map(d):
    d.cover(
        "MIZANPRO CRACK LAB · X32DBG · 2026",
        "كسر حماية ميزان Pro بـ x32dbg",
        "من الصفر إلى المية — دليل الفريق والمشرفين على الورشة",
        [("الفريق", "علي العطيفي · ماجد الدميني · المعتصم الراعي — دفعة 2026"),
         ("المادة", "الهندسة العكسية — المشروع العملي (فصل 4)"),
         ("المشرف", "م. عبدالكريم الباشيري"),
         ("الهدف", "ميزان Pro 3.0.0.0 — crackme من بنائنا، win-x86 / .NET 6"),
         ("الحجم", "عرض 35 دقيقة + تطبيق مباشر + إشراف على 3 مجموعات")],
        "حدود ما سنفعله",
        ["كل ما في هذا الدليل يُطبَّق على برنامج كتبناه نحن خصيصاً لهذا الغرض (crackme أكاديمي).",
         "تطبيق أي خطوة هنا على برنامج لا تملكه = مخالفة نظامية وأكاديمية؛ لا تفعلها، ولا تنشر أي أداة ناتجة عنها.",
         "سنُظهر **الثغرات ونوقش الدفاع ضدها** — هذه هي المادة، لا «كيف نسرق برنامجاً»."])

    d.new_chapter(0, "كيف تقرأ هذا الدليل", "ثلاث مسارات حسب الوقت المتبقي لك")
    d.para("الدليل مبني ليُقرأ من أوله لآخره في ساعة، لكن **غداً عندك عرض**. اختر مسارك الآن:")
    d.table(["المسار", "اقرأ", "الوقت", "لمن"],
            [["أ — النجاة السريعة", "الفصل 3 (تجهيز) ← 7 (الرتق) ← 13 (السيناريو) + الملحق (ب)", "30 د", "من لم يجرّب x32dbg إطلاقاً"],
             ["ب — الفهم الكامل", "1 ← 2 ← 4 ← 5 ← 6 ← 7 ← 8 ← 9 ← 10 ← 12", "90 د", "من يريد أن يشرح لا أن ينفّذ فقط"],
             ["ج — الإشراف على الطلاب", "4 ← 5 ← 10 ← 13 ← 14", "45 د", "من سيمرّ بين الأجهزة يوم العرض"]],
            widths=[30, 88, 16, 39], align=["R", "L", "R", "L"])
    d.h2("ما الذي سنصل إليه في نهاية الدليل")
    d.bullets([
        "أنت تعرف **لماذا** تنجح كل خطوة، لا ماذا تضغط فقط.",
        "عندك **ثلاثة كسور** مستقلة للأمان نفس الهدف: تزوير التخزين، رتق IL في الذاكرة، رتق دائم للملف — وكسر رابع «نظيف»: مولّد مفاتيح.",
        "عندك **أرقاماً مُتحقَّقاً منها** (لا استنتاجات): كل مفتاح، كل مجموع، كل نمط بايتات.",
        "عندك **خطة بديلة لكل مرحلة** — والبروفة المسجّلة التي تنقذك إن سقط العرض.",
        "عندك **ردوداً جاهزة** على أسئلة الدكتور والطلاب (الفصل 14)."])
    d.callout("note", "قبل أن تبدأ",
              ["كل أمر/اختصار مكتوب هنا تمّت مطابقته مع توثيق x64dbg الرسمي. **لا يوجد أمر «من عندي»** — وإن ظهر لك سلوك مختلف، فالسبب نسخة الأداة أو توقيت الإيقاف؛ راجع جدول «أخطاء x32dbg الشائعة» في الفصل 4.",
               "لا نستطيع تشغيل `MizanPro.exe` ولا `x32dbg` داخل بيئة الكتابة (لينكس، بدون .NET ولا واجهة). لذلك كل خطوة في هذا الدليل مكتوبة بصيغة **«تمرين ثم تأكيد»**: جرّبها مرة واحدة على جهاز العرض، وضع علامة ✔ بجانبها. أي خطوة لم تُجرَّب يجب أن تكون لها خطة بديلة مكتوبة."])
    d.h2("خريطة الورشة كما فُهمت من المشرف")
    d.table(["ما طُلب", "كيف ترجمناه", "الدليل في العرض"],
            [["كل مجموعة تختار برنامجاً أو برمجية خبيثة", "اخترنا **برنامجنا**: ميزان Pro — نظام محاسبة WPF فيه حماية ترخيص من ثلاث طبقات", "شجرة المشروع + `README.md`"],
             ["إما كسر الحماية أو تحليل سلوكي", "الاثنتان: **كسر** (IL/رتق/keygen) + **سلوكي** (سجل API + Registry + ملف الحالة)", "الفصول 6، 8، 9"],
             ["عرض 30–40 دقيقة أمام الطلاب", "14 فصلاً مضغوطة في 12 مقطع عرض، كل مقطع 2–4 دقائق", "الفصل 13"],
             ["تطبيق مباشر (Live demo)", "ثلاث لحظات «يُطبَّق أمامكم» مع مفتاح مُثبت الرياضيات", "الفصل 7 + 10"],
             ["الإشراف على مجموعات أخرى تفعل الشيء نفسه", "قائمة مراجعة + 5 أخطاء نتوقعها منهم + سلم تقدير", "الفصل 13/3"]],
            widths=[44, 78, 51], align=["R", "L", "R"])


# ═══════════════════════════════ 1 ═══════════════════════════════
def ch1(d):
    d.new_chapter(1, "ما معنى «كسر»؟ وما الذي نكسره بالضبط",
                  "المفردات والأنواع الستة — ولماذا اختيار الأداة هو نصف الحل")
    d.h1("١.  الحماية = فحص + قرار")
    d.para("كل حماية برمجية هي عبارة عن **فحص** ينتج عنه **قرار** (`true/false` أو «اسمح/امتنع»). الكسر ليس «إزالة الحماية» كما يصوّره الناس، بل هو **التحكم في أحد الطرفين**: إما أن تُرضي الفحص (تولّد مفتاحاً صحيحاً = keygen)، أو أن تُزيّف نتيجة الفحص (تعدّل الذاكرة/الملف = patch)، أو أن تُقنع طبقة التخزين (تكتب في الـ Registry/الملف ما يوهم بأنك مرخّص).")
    d.table(["الطبقة", "أين القرار", "طريقة الكسر المقابلة", "صعوبتها"],
            [["مفتاح تسلسلي محلي", "دالة تحقق في نفس الملف", "keygen أو رتق الدالة", "سهلة جداً"],
             ["فترة تجربة", "مقارنة تاريخ + عدّاد", "تعديل التاريخ المخزَّن", "سهلة"],
             ["تنشيط مرتبط بالجهاز", "ملف/قيمة مخزّنة مشفّرة", "تزوير التخزين (إن كان المفتاح في البرنامج)", "متوسطة"],
             ["تنشيط من خادم", "خارج الجهاز", "لا شيء — إلا شراء الترخيص", "مغلقة عملياً"],
             ["حصان طروادة/تشفير كامل للوظيفة", "المفتاح مشتق من شيء لا تملكه", "استخراج المفتاح من الذاكرة وقت التشغيل", "صعبة"],
             ["حماية تجارية (VMProtect/DNA…)", "آلة افتراضية + فحوص ذاتية", "unpacking + anti-anti-debug", "مشروع كامل"]],
            widths=[34, 46, 60, 26], align=["R", "L", "L", "R"])
    d.callout("ok", "القاعدة الذهبية",
              ["اسأل دائماً: **«أين تُتَّخذDecision؟»**. كل كسر ناجح هو جملة واحدة: «القرار يُتَّخذ في الدالة X ← سأجعل X تُرجع ما أريد».",
               "في ميزان Pro الجواب محدد وحرفي: الدالة `TokenProcessor.ParseAndVerify` تُرجع `bool`، والرتق الذي يجعلها تُرجع `1` دائماً = كسر كامل."])

    d.h1("١.٢  قاموس 90 ثانية")
    d.kv([("Crackme", "برنامج كُتب ليُكسر — هدف تعليمي، لا جريمة فيه."),
          ("Keygen", "برنامج يشتق مفتاحاً صالحاً بنفس الخوارزمية. لا يغيّر الملف المستهدف إطلاقاً."),
          ("Patch", "تعديل بايتات (في الملف أو في الذاكرة) لتغيير منطق القرار."),
          ("Patcher", "أداة تطبّق الرتق على نسخة الهدف وتحفظها."),
          ("Nag / Trial", "تذكير «انتهت التجربة»؛ إزالته تسمى أحياناً nag-slapping."),
          ("HWID / بصمة الجهاز", "بصمة تُشتق من العتاد لربط الترخيص بجهاز واحد."),
          ("IL", "لغة الوسيط في .NET — الأوامر «المصغّرة» التي يولّدها المترجم C#."),
          ("JIT", "المترجم اللحظي: يحوّل IL إلى كود آلة عند أول تنفيذ (أو الثاني) للطريقة."),
          ("RVA / FOA", "عنوان نسبي داخل الصورة / إزاحة داخل الملف — الجسر بين ما تراه في الذاكرة وما تراه في المحرّرHex.")])
    d.callout("note", "لماذا نهتم بـ .NET أصلاً؟",
              ["لأن نصف «أساليب الكسر» الكلاسيكية (التي وُلدت في زمن DOS/Win32 و`asm` الخام) تعمل هنا **بطبقة ترجمة**: بدل أن تعدّل `mov eax, 1` تعدّل `ldc.i4.1`، وبدل أن تبحث عن الكود في `.text` تبحث في تيار `#Text` عن **محتوى جسم الطريقة**. و`x32dbg` يرى الطبقة السفلى فقط — وهذا بالضبط ما سنجعله نقطة قوتنا لا نقطة ضعفنا (الفصل 7)."])

    d.h1("١.٣  الإطار القانوني والأخلاقي (قله بصوت عالٍ غداً)")
    d.bullets([
        "المشروعية تأتي من **ملكية الهدف**: البرنامج من بنائنا، الغرض تعليمي، ولا توزيع لأدوات كسر لأطراف ثالثة.",
        "لا نستخدم ولا ننشر أي شيء يخص برامج تجارياً محمية؛ وإن ذكر طالب مثالاً واقعياً، نحوّله فوراً إلى «كيف ندافع؟».",
        "في التقرير النهائي نكتب صراحة: الهدف `MizanPro` crackme، والغرض تعليمي، ولا آثار على أنظمة الغير.",
        "الأخلاقي الأهم في مادتنا: **كل ثغرة نشرحها = إصلاح نقترحه**. لا يوجد «هك وامشِ» — انظر الفصل 12."])


# ═══════════════════════════════ 2 ═══════════════════════════════
def ch2(d):
    d.new_chapter(2, "تشريح الملف: ما الذي يُنفَّذ فعلاً",
                  "PE · apphost · حزمة أحادية الملف · IL · JIT — أربع دقائق تنقذك من ساعة تخمين")
    d.h1("٢.١  طبقتان: ملف التنفيذ والكود المُدار")
    d.para("`MizanPro.exe` (win-x86) الذي تشغّله ليس هو البرنامج — هو **apphost**: قشرة صغيرة مهمتها تحميل `hostfxr` ثم `hostpolicy` ثم `coreclr.dll` (نواة CoreCLR) وتمرير مسار الـ DLL المُدار. **منطقك كله يعيش في `MizanPro.dll`** (عند النشر بدون ملف واحد) داخل قسم `.text` في تيار بيانات اسمه `#Text` (IL + metadata).")
    d.table(["الكيان", "ما هو", "ما يراه x32dbg", "ما يراه dnSpy"],
            [["MizanPro.exe", "apphost أصلي 32-بت", "كود آلة + نقاط توقف ممكنة (بلا معنى لمصلحتنا)", "لا يعرض IL مضمّناً"],
             ["coreclr.dll / clrjit.dll", "نواة .NET + المترجم اللحظي", "آلاف الإطارات «غرباء» في المكدّس", "—"],
             ["MizanPro.dll", "تجميعتك المُدارة (IL)", "منطقة في خريطة الذاكرة + تيار `#Text`", "الشجرة الكاملة: أنواع، طرق، IL، C#"],
             ["حزمة أحادية الملف", "كل ما سبق مضغوط داخل exe", "لا `MizanPro.dll` على القرص", "يفشل الفتح — انظر ٢.٣"]],
            widths=[34, 40, 62, 50], align=["R", "L", "L", "L"])

    d.h1("٢.٢  من C# إلى IL إلى آلة — ولماذا «التوقيت» يحكم")
    d.code_block("""// المصدر (Core/Engine/TokenProcessor.cs — السطر 89)
return sum % 73 == 0 && xor == RequiredXor;

// IL الذي سيظهر في dnSpy (مبسّط — المطلوب منكم فقط قراءة هذا الجزء)
IL_00b1:  ldc.i4.s   73      ; 1F 49        ← حمل الثابت 73
IL_00b3:  rem                ; D0            ← sum % 73
IL_00b4:  brfalse.s  IL_00c4 ; 2C 0E         ← إن كان الباقي ≠ 0 اقفز إلى «false»
IL_00b6:  ...                ;               ← حلقة الـ XOR
IL_00c0:  ldc.i4.s   10      ; 1F 0A         ← RequiredXor (const → مضمّن)
IL_00c2:  bne.un.s   IL_00c4 ; 39 FC         ← إن اختلف اقفز إلى «false»
IL_00c4:  ...
IL_00c5:  ldc.i4.1 / ldc.i4.0 ; 17 / 16     ← القيمة المُرجَعة
IL_00c6:  ret                 ; 2A""",
                 title="الهدف: ثلاثة بايتات فقط — 1F 49 D0")
    d.para("لاحظ أن `RequiredXor` معرّف `const byte` — لذا **يُضمَّن** في IL ولا يُولّد `ldsfld`. يعني ذلك أن البت `0A` قابل للتعديل هو الآخر، لكننا سنختار الهدف `73` لأنه (أ) فريد في الملف، (ب) تغييره إلى `1` يجعل **الشرط حسابياً دائماً الصدق** بدون أن يلمس أي شيء آخر.")
    d.callout("danger", "أهم جملة في الدليل كله",
              ["**CoreCLR لا يترجم الطريقة إلا عند أول استدعاء لها** (وللمرة الأولى يترجمها المترجم السريع `tier0`، ثم قد يعيد ترجمتها `tier1` لاحقاً). النتيجة العملية: تعديل صورة IL في الذاكرة **بعد** أن تكون `ParseAndVerify` قد تعمل مرة واحدة = **لا شيء يتغيّر** — لأن كود الآلة الخاص بها وُلد من نسخة IL القديمة.",
               "لهذا في الفصل 7 سنرتق **قبل** أول محاولة تفعيل، وسنشرح للطلاب لماذا تُجرَّب هذه التجربة مرتين (قبل/بعد) — فهي وحدها تشرح معنى JIT."])
    d.h1("٢.٣  إعداد النشر الذي اخترناه — ولماذا")
    d.code_block("""<!-- MizanPro.csproj — التوصية النهائية للورشة -->
<RuntimeIdentifier>win-x86</RuntimeIdentifier>
<SelfContained>true</SelfContained>            <!-- ابقِها: لا نريد تثبيت .NET Runtime على 20 جهازاً -->
<PublishSingleFile>false</PublishSingleFile>   <!-- غيّرها من true: هذا سطر يُنقذ الورشة -->
<DebugType>none</DebugType>                    <!-- لا PDB: يقرّبنا من برنامج «حقيقي» -->
<Optimize>false</Optimize>                     <!-- IL خام: أسهل للقراءة في dnSpy -->

:: البناء
dotnet publish -c Release -r win-x86 --self-contained true ^
               -p:PublishSingleFile=false -o dist""",
                 title="الحزمة التي سنحلّلها", note="بعد البناء يجب أن ترى `dist\\MizanPro.dll` — إن لم تره فالتغيير لم يُطبَّق، وأوقف كل شيء.")
    d.table(["الإعداد", "مع `SingleFile=true` (الحالي)", "مع `SingleFile=false` (المطلوب)"],
            [["ما هو على القرص", "`MizanPro.exe` واحد ~150MB", "`MizanPro.exe` صغير + `MizanPro.dll` + ~250 مكتبة"],
             ["هل يفتح dnSpy الهدف؟", "لا (الحزمة غير مدعومة للتحليل/الحفظ)", "نعم — يفتح `MizanPro.dll` ويعرض IL"],
             ["«Save Module» بعد التعديل", "مستحيل تقنياً", "يعمل ويُنتج ملفاً معدَّلاً"],
             ["مكتبات `System.*` لمرجعيات dnSpy", "داخل الحزمة", "بجانب DLL ← محرّر C# يستطيع حلّها"],
             ["ما يراه المنقّح في خريطة الذاكرة", "الحزمة في `MizanPro.exe` فقط", "منطقة باسم `MizanPro.dll` (نبحث فيها)"],
             ["هل يحتاج الطالب تثبيت Runtime؟", "لا (self-contained)", "لا (self-contained)"]]
            , align=["R", "L", "L"])
    d.callout("warn", "لا تنسَ التبعات",
              ["`Workshop_Pack.bat` ينسخ **ملفاً واحداً** الآن — يجب أن ينسخ محتويات `dist\\` كاملة، و`README_Workshop.md`/`LabSheet.md` يجب أن تطلب تشغيل `MizanPro\\MizanPro.exe` لا «ملفاً واحداً». وإلا سيستلم الطلاب ملفاً لا يعمل.",
               "في إصدار 32-بت من apphost ستجد `coreclr.dll` (وليس `clr.dll` القديم) في خريطة الذاكرة — لا تكتب `clr.dll` على السبورة."])

    d.h1("٢.٤  متغيّرات بيئة نُثبّتها لنرى IL بوضوح")
    d.code_block(""":: قبل تشغيل x32dbg (نافذة cmd واحدة تكفي للجلسة كلها)
set DOTNET_TieredCompilation=0   :: لا إعادة ترجمة tier1 ← عناوين الكود الأصلي لا تتغير أثناء الجلسة
set DOTNET_ReadyToRun=0          :: لا كود أصلي مُسبق داخل الحزمة ← كل طريقة تُترجم في وقتها
set DOTNET_gcServer=0            :: سلوك GC معتاد (أقل «مفاجآت» في Threads view)

:: اختياري: إظهار معلومات JIT في Log
set DOTNET_JitDisasm=ParseAndVerify   :: يحتاج build مع_DIAGNOSTICS؛ للمتقدمين فقط""",
                 title="cmd · ضبط البيئة للبروفة",
                 note="السطران الأولان هما المهمّان: يجعلان «ما تراه في الذاكرة = ما ستراه بعد لحظات» — وشرط أي شرح على مستوى كود الآلة.")


# ═══════════════════════════════ 3 ═══════════════════════════════
def ch3(d):
    d.new_chapter(3, "تجهيز المختبر من الصفر",
                  "جهاز العرض، الأدوات، المجلدات، والتنظيف قبل كل تجربة")
    d.h1("٣.١  قائمة التثبيت (افعلها الليلة)")
    d.table(["الأداة", "ماذا نأخذ", "لماذا في خطتنا", "بديل إن تعذّر"],
            [["x64dbg (v50+) · `x32dbg.exe`", "ZIP الرسمي → استخرِجه ثم شغّله", "النجم: breakpoints + ذاكرة + سجلات + رتق IL", "OllyDbg2 (محدود) — لا يُنصح"],
             ["dnSpyEx", "آخر إصدار من المستودع", "قراءة IL/تعديل جسم الطريقة وحفظ الملف", "ILSpy (قراءة فقط + Some edit)"],
             ["HxD", "الموقع الرسمي", "بحث/تديل بايتات على القرص (`MizanPro.dll`)", "أي محرّر hex"],
             ["PE-bear أو CFF Explorer", "المستودع", "رؤية الأقسام/`#Text` والربط RVA↔ملف", "حساب يدوي من جدول الأقسام"],
             ["Procmon", "Sysinternals", "دليل «ماذا يكتب التطبيق فعلاً» (Registry/ملفات)", "رسائل x32dbg log-only"],
             ["Python 3 + `pycryptodome`", "`pip install pycryptodome`", "تزوير `state.enc` (AES-CBC + Base64)", "C# console صغير"],
             ["OBS Studio", "الموقع الرسمي", "الفيديو الاحتياطي الذي ينقذ العرض", "Xbox Game Bar"]],
            widths=[34, 34, 66, 44], align=["R", "L", "L", "L"])
    d.callout("danger", "ثلاث أخطاء تُضيّع ساعة قبل العرض",
              ["تشغيل `x32dbg.exe` من داخل الـ ZIP بدون استخراج → «missing dll / app crash». استخرِج المجلد كاملاً.",
               "فتح `x64dbg.exe` لهدف `win-x86` → لن يرى ما تتوقعه (أو يرفض). هدفنا 32-بت ← `x32dbg.exe`.",
               "مضاد فيروسات يحذف مجلد x64dbg أو `MizanPro.exe` (packer/behavior) → استثنِ المجلد `C:\\Lab\\` وأوقف الحماية اللحظية أثناء البروفة فقط."])

    d.h1("٣.٢  مجلدات مرتّبة (هيكلاً، لا عشوائياً)")
    d.code_block("""C:\\Lab\\
   MizanPro\\            ← dist\\ كاملاً (MizanPro.exe + MizanPro.dll + المكتبات)
   x32dbg\\Release\\      ← المستخرَج
   tools\\               ← HxD.exe, dnSpy.exe, PE-bear.exe, Procmon64.exe
   keygen.py            ← من الفصل 10 (جرّبه يعمل قبل الغد)
   forge_state.py       ← من الفصل 6
   reset-license.bat    ← من ٣.٤
   shots\\               ← لقطات العرض (نفس دقة جهاز العرض)
   backup\\              ← MizanPro.dll النظيف + MizanPro_patched.dll""",
                 title="الهيكل المقترح")
    d.para("سبب الترتيب ليس جمالياً: كل مسار طويل تكتبه أثناء العرض هو ثانية صمت أمام 60 طالباً، وكل ملف «على سطح المكتب» يعني أنك ستجد نفسك تبحث في قائمة الإكمال التلقائي عن `C:\\Users\\شمه\\Desktop\\...`.")

    d.h1("٣.٣  إعدادات x64dbg الافتراضية (افعلها مرة واحدة للأبد)")
    d.bullets([
        "**Options → Preferences → Events**: فعّل `System breakpoint` (نقطة توقف عند تحميل `ntdll` — هنا تكون الـ DLLs الرئيسية محمّلة)، وأبقِ `Entry point` مفعّلاً أيضاً.",
        "**Options → Preferences → Exceptions**: أضف `0xE0434352` (CLR first-chance) إلى القائمة المتجاهَلة — وإلا سيتوقف المنقّح كل 3 ثوانٍ بسبب استثناءات CLR الداخلية.",
        "**Options → Preferences → Appearance**: حجم الخط 12–14 للـ Disassembly و9–10 للـ Log (البروجكتور «يأكل» الحروف الصغيرة).",
        "**Options → Preferences → General**: عطّل `Confirm on exit` حتى لا يضيع منك توقيت الانتقال بين الشاشات.",
        "احفظ الإعدادات (`dbsave` للتعليقات/العلامات داخل جلسة الهدف، و`Config` للأداة نفسها) وانسخ المجلد إلى جهاز الاحتياط."])

    d.h1("٣.٤  التنظيف قبل كل تجربة (script واحد)")
    d.code_block("""@echo off
REM reset-license.bat — يعيد ميزان Pro إلى «نسخة جديدة على هذا الجهاز»
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob /f >nul 2>&1
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate     /f >nul 2>&1
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\Settings" /f                     >nul 2>&1
del  /f /q "%APPDATA%\\MizanPro\\state.enc"                                          2>nul
echo MizanPro license state cleared.
pause""",
                 title="C:\\Lab\\reset-license.bat",
                 note="ملاحظة 32/64-bit: `HKCU\\Software` غير محوَّل (لا يوجد Wow6432Node للـ HKCU) — القيمة التي يكتبها تطبيق 32-بت تراها `regedit` في نفس المكان.")
    d.h2("ثلاث حالات نحتاجها في العرض")
    d.code_block(""":: (أ) نسخة جديدة تماماً
reset-license.bat

:: (ب) «انتهت التجربة» — لإظهار نافذة الانتهاء (الفصل 8)
reg add "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate /t REG_SZ /d 20250101 /f

:: (ج) «مُفعَّل جاهز» — للقطات وللبدايات السريعة
reg add "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob /t REG_SZ /d M1Z4N-2025P-ROM4S-T3R01 /f""",
                 title="cmd · ثلاث حالات جاهزة")


# ═══════════════════════════════ 4 ═══════════════════════════════
def ch4(d):
    d.new_chapter(4, "جولة كاملة في x32dbg — قبل أن نلمس الهدف",
                  "الواجهة، 24 اختصاراً، أنواع نقاط التوقف، والفخاخ العشرة")
    d.h1("٤.١  ما الذي تراه عند أول F9")
    d.para("بعد `File → Open (F3)` واختيار `MizanPro.exe` يتوقف x32dbg عند **System Breakpoint**: أنت داخل `ntdll`، قبل أن يصل الكود إلى `main`، والصور المحمّلة حتى الآن قليلة. اضغط `F9` لتتمة الإقلاع؛ ستمرّ بنقطة `Entry point`، ثم تظهر نافذة البرنامج. **هنا نريد أن نكون قد وصلنا**: الـ `MizanPro.dll` أصبح في الذاكرة، لكن طرائقنا لم تُنفَّذ بعد.")
    d.table(["اللوحة", "اختصار فتحها", "ما نفعها في ورشتنا"],
            [["CPU / Disassembly", "Alt+C", "الأوامر الأصلية + Space للتعديل المحلي + `;` للتعليق"],
             ["Registers", "—", "تعديل `eax` (تمرين الفصل 8) ورؤية الأعلام"],
             ["Call Stack", "Alt+K", "إثبات أن النداء جاء من `coreclr`/JIT، لا من كودك"],
             ["Memory map", "Alt+M", "العثور على منطقة `MizanPro.dll` — بوابة الفصل 7"],
             ["Breakpoints", "Alt+B", "شرط/سجل/مرة واحدة/حذف (`bc`)"],
             ["Log", "Alt+L", "دليل «رأى/لم يرَ» — أهم لوحة لإقناع الجمهور"],
             ["Patches", "Ctrl+P", "مراجعة كل ما رتّقته في هذه الجلسة"],
             ["Notes", "Alt+N", "ملاحظاتك فوق العناوين — تُحفظ مع `dbsave`"]],
            widths=[36, 24, 113], align=["R", "R", "L"])

    d.h1("٤.٢  الأوامر التي سنستعملها فعلاً (ومعناها الحرفي)")
    d.table(["مفتاح/أمر", "ما يفعله", "متى نستخدمه"],
            [["F9", "تشغيل / متابعة", "بعد كل نقطة توقف"],
             ["F7 / F8", "خطوة داخل / خطوة فوق", "المشي سطر-سطر داخل IL مُترجَم"],
             ["Ctrl+F9", "تنفيذ حتى العودة (`ret`) من الطريقة الحالية", "لحظة التقاط `eax`"],
             ["Alt+F9", "تشغيل حتى «كود المستخدم»", "للخروج من زحام `ntdll`/`coreclr`"],
             ["F4", "تشغيل حتى السطر المحدد", "«لا أريد نقطة توقف، فقط تابع إلى هنا»"],
             ["F12", "إيقاف مؤقت (pause)", "بعد ظهور النافذة، قبل البحث في الذاكرة"],
             ["F2", "تبديل نقطة توقف على السطر الحالي", "على `bp user32.MessageBoxW` مثلاً"],
             ["Ctrl+G", "اذهب إلى عنوان/اسم", "`user32.MessageBoxW` أو `MizanPro.dll+1234`"],
             ["Ctrl+F / Ctrl+B / Ctrl+E", "بحث نصّي / بحث ثنائي (pattern) / تعديل ثنائي", "الفصل 7: نمط `1F 49 D0`"],
             ["Space", "فتح نافذة Assemble على السطر المحدد", "لتسجيل كيف يبدو `ldc.i4.s 1`"],
             [";  / :", "إضافة تعليق / تسمية (label)", "حتى يفهم الجمهور شاشتك"],
             ["Ctrl+D / Ctrl+Backspace", "علامة مرجعية / الرجوع للتحديد السابق", "التنقل السريع أمام الطلاب"],
             ["Ctrl+F2 / Alt+F2", "إعادة تشغيل الهدف / إغلاقه", "لتجربة «قبل/بعد» بسرعة"],
             ["`bp`, `bc`, `bph`, `bpm`", "نقاط توقف عادية / مسح الكل / على ندّاء / على كتابة ذاكرة", "انظر ٤.٣"],
             ["`log`, `msg`, `find`, `set`", "كتابة في Log / رسالة / بحث في الذاكرة / تعديل قيمة", "كل شيء يوثَّق بصرياً"]],
            widths=[36, 66, 71], align=["L", "L", "L"])
    d.callout("danger", "أوامر **لا توجد** في x64dbg",
              ["`bpdll` (موجود في `CrackGuide.md` — احذفه)، `bpx`/`bpm` بأسلوب OllyDbg المعطوف، `Run till return` كسطر أوامر (الاسم `ret`/Ctrl+F9).",
               "الكتابة الصحيحة في شريط الأوامر: `bp user32.MessageBoxW` — والـ `;` في آخره لتعليق. أي أمر آخر سيظهر `unknown command` أمام الجمهور."])

    d.h1("٤.٣  نقاط التوقف: الأنواع الأربعة التي ستحتاجها")
    d.bullets([
        "**Software breakpoint (F2 / `bp`)**: يستبدل `int3` (0xCC) مؤقتاً. هو الافتراضي، وهو كافٍ لكل ورشتنا.",
        "**Conditional**: كليك يمين على الـ breakpoint ← Edit ← `Condition = eax==1` — لتتوقف فقط عند الشرط المهم (مثلاً: عند أول رسالة تحتوي «انتهت» بدل كل رسالة).",
        "**Log-only (الأناقة الحقيقية)**: `Log text = {p:arg1}` + `Break = Never` + `Command = gc` (go and count) ← يصبّ نصوص الرسائل في `Alt+L` **دون أن يوقف البرنامج** — أقوى لحظة «سلوكية» يمكنك عرضها.",
        "**Hardware / memory (`bph`, `bpm`)**: على كتابة/قراءة عنوان. مفيد في native، **غير مناسب** لمتغيّر مُدار في .NET (عنوانه يتغيّر مع GC) — لا تقع فيه غداً."])
    d.callout("note", "DLL لم تُحمَّل بعد؟",
              ["إذا أردت نقطة توقف على `user32.dll!MessageBoxW` في بداية الجلسة، لا تحتاج أمراً سحرياً: اذهب إلى **Options → Preferences → Events → `Break on DLL load`**، تابع حتى يُحمَّل الـ DLL (ستراه في Log)، ثم عطّل الخيار واضبط الـ `bp`. عملياً: `user32` محمّل أصلاً عند System Breakpoint في تطبيقات WPF، فغالباً لن تحتاج ذلك.",
               "الأسلوب الأبسط للهدف المُدار: **لا تنتظر DLL — انتظر أول نقرة** (الفصل 7)."])

    d.h1("٤.٤  خريطة الذاكرة والبحث الثنائي (مهارتك الأساسية غداً)")
    d.steps([
        "بعد ظهور نافذة التطبيق اضغط **F12** (إيقاف مؤقت). ستجد RIP داخل `ntdll` — لا يهم.",
        "**Alt+M** (Memory map) → ابحث في العمود «Module» عن `MizanPro.dll` → كليك يمين ← `Follow → In Disassembler` (أو في Dump).",
        "في نافذة الذاكرة: **Ctrl+B** (Binary search) واكتب `1F 49 D0`، وحدّد النطاق «Current module» إن طُلب منك، ثم `Find all`.",
        "انقر نتيجتك، تحقق من البايت الذي يليها (`2C` = `brfalse`) لتعرف أنك في المكان الصحيح (الفصل ٥.٢).",
        "**Ctrl+E** يفتح محرّر الذاكرة الثنائية ← عدّل البت `49` إلى `01` ← OK. افتح **Ctrl+P** لترى الرتق مسجلاً.",
        "**F9** تابع، ولا تُغلق x32dbg بعد التجربة — `Patches` تظهر لك «ما غيّرته» في حالة أردت إعادته."],
        size=10.0)
    d.callout("ok", "كيف تعرف أن الكتابة في الذاكرة ستقبلها Windows؟",
              ["صورة الـ DLL محمّلة بخصائص `write-copy`؛ أول كتابة **تنسخ الصفحة** ثم تعدّل النسخة الخاصة بالعملية. لذلك: (١) لن ترى `Access violation`، (٢) الملف على القرص **لا يتغيّر**، (٣) بعد `Detach` أو إعادة التشغيل تعود الأمور كما كانت. هذه بالضبط خواص «الرتق المؤقت» التي سنستغلها في الفصل 7."])

    d.h1("٤.٥  أخطاء x32dbg الشائعة — جدول إنقاذ")
    d.table(["العَرَض على الشاشة", "التشخيص", "العلاج"],
            [["البرنامج يشتغل ولا يتوقف أبداً عند نقطة IL", "الطريقة تُرجمت قبل ضبط الـ breakpoint (JIT)", "إعادة التشغيل (Ctrl+F2) ثم اضبط النقطة قبل الاستخدام؛ أو رتق IL كما في الفصل 7"],
             ["`Access violation`/توقف داخل `ntdll` كل ثانية", "استثناءات CLR first-chance", "تجاهل `0xE0434352` في Exceptions"],
             ["`unknown command`", "أمر من OllyDbg/Windbg", "راجع ٤.٢"],
             ["لا يظهر `MizanPro.dll` في Memory map", "النشر أحادي الملف (لا يوجد DLL) أو لم يُحمَّل بعد", "غيّر إعداد النشر؛ أو تابع بـ F9 حتى Log يخبرك بالتحميل"],
             ["النتائج كثيرة لبحث `1F 49 D0`", "نمط قصير يظهر في تواريخ/جداول أخرى", "ابحث `1F 49 D0 2C`، وحدّد النطاق داخل موديول `MizanPro.dll`"],
             ["الـ Disassembly يعرض `???` أو أكواد غريبة", "أنت في كود مُولّد من JIT لا يمكن تفكيكه كـ IL", "هذا طبيعي — لا تشرح IL من هنا؛ افتح dnSpy للعرض التعليمي"],
             ["`bad copy of the executable` عند F3", "ملف مضغوط/مقفول أو AV تدخّل", "استخرج الهدف في مجلد مستثنى، وأوقف الحماية اللحظية"],
             ["لا أرى الـ Unicode text في الذاكرة", "نص UTF-16 معرّب لا يظهر في «ASCII string»", "استخدم Follow in Dump ثم «Unicode» — أو `find` ببايتات UTF-16"],
             ["سقطت الجلسة بعد `Detach`", "CLR لا يحب بعض حالات الإنهاء", "أغلق العملية من x32dbg (Alt+F2) ثم أعد تشغيلها يدوياً"],
             ["الـ Log ممتلئ بـ `Load …` ولا جديد", "أنت في وضع «Log يصرخ» مع DLL load breakpoints", "عطّل `Break on DLL load` وامسح Log (كليك يمين → Clear)"]],
            widths=[54, 56, 63], align=["L", "L", "L"])
