#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""الفصول 10–14 + الملاحق."""


# ═══════════════════════════════ 10 ═══════════════════════════════
def ch10(d):
    d.new_chapter(10, "Keygen من الصفر — الرياضيات ثم الكود",
                  "برهان استحالة 0x2A، قاعدة الـ 146، ومولّد مفاتيح باسم جامعتك")
    d.h1("١٠.١  ما الذي تحسبه الدالة فعلاً؟")
    d.para("بعد حذف الشرطات والتحويل لأحرف كبيرة، المفتاح 20 محرفاً من `A-Z0-9`. الشرطان: **مجموع الأكواد** يقبل القسمة على 73، و**XOR التراكمي** يساوي `0x0A`. لا ترتيب ولا موضع — أي تبديل للمحارف يبقي المفتاح صالحاً. هذه معلومة ثمينة: تعني أن المساحة «مفلترة» بشرطين فقط، وأن **أي** تركيب يحققهما مقبول.")
    d.code_block("""محارف الأرقام 0-9 : 0x30..0x39  →  البت 6 = 0 ، البت 5 = 1
محارف الأحرف A-Z : 0x41..0x5A  →  البت 6 = 1 ، البت 5 = 0

في XOR النهائي لـ 20 محرفاً:
   البت 6 = زوجية عدد الأحرف
   البت 5 = زوجية عدد الأرقام
   وعدد الأحرف + عدد الأرقام = 20 (زوجي) ⇒ الزوجيتان متساويتان ⇒ البت 6 = البت 5 دائماً
   ⇒ القيمتان الوحيدتان الممكنتان للبتين: 00 أو 11

المطلوب 0x0A = 0b0000_1010 → البت6=0 ، البت5=0 ✓ ممكن
المواصفة الأصلية طلبت 0x2A = 0b0010_1010 → البت6=0 ، البت5=1 ✗ مستحيل (تناقض)""",
                 title="برهان استحالة 0x2A — أربع سطور تُنهي النقاش")
    d.callout("ok", "ثغرة في «الحل» الذي اعتُمد — اكتشفها الطلاب معنا",
              ["من استحالة `0x2A` نستنتج أن **بتّي الـ XOR مرتبطان** بأي أبجدية `[A-Z0-9]` ذات طول زوجي ⇒ الحماية «لا تملك» سوى نصف فضاء البتات. والأخطر: لأن `bit0(xor) = 0` (القيمة `0x0A`) فإن **عدد المحارف الفردية زوجي** ⇒ **مجموع الأكواد زوجي** ⇒ وبما أن المجموع يجب أن يقبل القسمة على 73 (فردي)، فاللازم:",
               "**المجموع يجب أن يكون مضاعفاً لـ 146، لا لـ 73** ⇒ القيم الممكنة: 1022، 1168، 1314، 1460، 1606، 1752 (وكل مفتاح معروف عندنا مجموعه 1314 أو 1460 ✓).",
               "النتيجة العملية: كثافة المفاتيح الصالحة ≈ `1/146 × 1/128 ≈ 1/4672` من فضاء 20 محرفاً، لا `1/73`. قل هذه على المسرح؛ إنها بالضبط نوع الملاحظة التي تُظهر هندسة لا ترحميلاً."])

    d.h1("١٠.٢  الكود الأول: محاكاة الفحص (نفحص به أي مفتاح)")
    d.code_block("""# keygen.py — محاكاة دقيقة لـ TokenProcessor.ParseAndVerify
import string, random
ALPHA = string.ascii_uppercase + string.digits        # [A-Z0-9]  (36 خياراً)
MOD, XOR_TARGET, LENGTH = 73, 0x0A, 20

def clean(key):            return key.replace("-", "").upper()
def verify(key: str) -> bool:
    c = clean(key)
    if len(c) != LENGTH:                    return False
    if any(ch not in ALPHA for ch in c):    return False
    total, x = sum(map(ord, c)), 0
    for ch in c:                            x ^= ord(ch)
    return total % MOD == 0 and x == XOR_TARGET

def fmt(key):              return "-".join(key[i:i+5] for i in range(0, 20, 5))

if __name__ == "__main__":
    for k in ["M1Z4N-2025P-ROM4S-T3R01", "5NOFK-JQB1Z-7HSHF-NOP6D"]:
        c = clean(k); t = sum(map(ord, c))
        print(f"{k:24} sum={t:5d}  %73={t%73:2d}  valid={verify(k)}")""",
                 title="C:\\Lab\\keygen.py (1/2) — نختبر به أولاً",
                 note="المخرجات المتوقعة عندك: السطر الأول valid=True، والثاني valid=False — إن خرج العكس فأنت تكتب سكربتاً مختلفاً.")

    d.h1("١٠.٣  الكسر النظيف: مولّد يعمل في أقل من ثانية")
    d.code_block("""# keygen.py (2/2) — القوة الغاشمة + نسخة «مفتاح مزخرف» باسم الفريق
import random

def brute_key():
    while True:
        k = "".join(random.choice(ALPHA) for _ in range(20))
        if verify(k):
            return k                     # متوسط ~4.7 ألف محاولة، أقصى ما قِسناه 13.8 ألف

def vanity_key(prefix: str, tail: int = 5):
    '''ثبّت بادئة (حتى 15 محرفاً) وابحث عن ذيل عشوائي — يعمل في ميلي‑ثوانٍ.'''
    p = "".join(ch for ch in prefix.upper() if ch in ALPHA)[:LENGTH - tail]
    p = p.ljust(LENGTH - tail, "X")
    while True:
        k = p + "".join(random.choice(ALPHA) for _ in range(tail))
        if verify(k):
            return fmt(k)

print(vanity_key("MIZAN2026X32DBG"))   # → MIZAN-2026X-32DBG-B36SQ  (مثبت)
print(brute_key())""",
                 title="C:\\Lab\\keygen.py (2/2) — جرّبه الآن",
                 note="مفاتيح جاهزة مُولّدة ومُتحقَّق منها (انسخها في العرض): `MIZAN-2026X-32DBG-B36SQ` · `CRACK-MEWOR-KSHOP-BY0A5` · `MIZAN-PROCR-ACKED-P4P7L` · `GROUP-4SENI-OUR00-JYIP2` · `ALBLA-SATEA-M4000-6LFK4` — كلها sum ∈ {1314,1460} و xor = 0x0A.")

    d.h1("١٠.٤  تصحيح مهم في `LabSheet.md` (قله للمجموعة المشرفة)")
    d.para("المسودة تقترح: «ثبّت 18 محرفاً واشتق آخر 2 بحلّ المعادلتين». **هذا غالباً لا يحلّ**. القياس على 300 بادئة عشوائية بأبجدية `[A-Z0-9]` (مع فحص كل الاحتمالات للذيل، وليس عيّنة):")
    d.table(["الخطة", "مساحة البحث", "نسبة البادئات التي لها حل", "الحكم"],
            [["ثبّت 18 ← اشتق 2", "36² = 1296", "≈ 7%", "✗ غير موثوق"],
             ["ثبّت 17 ← اشتق 3", "36³ = 46656", "≈ 38%", "⚠ لا يكفي"],
             ["ثبّت 16 ← اشتق 4", "36⁴ = 1.68M", "≈ 72%", "⚠ يبقى 28% بلا حل"],
             ["ثبّت 15 ← ذيل عشوائي 5", "≈ 4.7 ألف محاولة", "100% (عملياً)", "✓ استخدم هذا"]],
            widths=[36, 30, 40, 34], align=["L", "L", "L", "R"])
    d.callout("note", "لماذا تفشل «المعادلتان بجهولين»؟",
              ["لأن القيدين ليسا مستقلين: لكل محرف في `[A-Z0-9]` علاقة صارمة بين قيمته الحسابية وبتّيه 5/6، فليست كل أزواج (مجموع، XOR) قابلة للتحقيق. الرياضيات النظيفة تقول: بمعلومية `a⊕b = T` فإن `a+b = T + 2(a·b)` ⇒ المجموع محصور بين قيم قليلة لا تغطي كل المود 146.",
               "هذا ليس «عيباً في الواجب» — هو **اكتشاف** تكتبه في تقريرك: «طبّقنا طريقة الورقة ففشلناها في 93% من البادئات، وقسنا السبب وقترحنا البديل». أقوى من حلّ التمرين حرفياً."])

    d.h1("١٠.٥  تمرين 5 دقائق للطلاب (أدره هكذا)")
    d.steps([
        "أعطِ كل طالب السطر الأول من `keygen.py` (20 سطراً) — لا تعطه الحل.",
        "المطلوب: مفتاح يبدأ باسمه هو (مثلاً `SARAH2026...`) بطول 20 — يستخدم `vanity_key` أو يعدّل الذيل يدوياً.",
        "الأول الذي ينجح يشرح على السبورة **كيف عرف أن المجموع يجب أن يكون زوجياً** (قاعدة 146) — هذه هي الدرجة.",
        "ثم اطلب فشلين مقصودين: مفتاح فيه `-` زائدة في الآخر، ومفتاح فيه حرف `s` صغير ⇒ سيقبلهما التطبيق (ToUpperInvariant + إزالة الشرطات) — اسأل: «هل هذا آمن؟».",
        "اختم بأن كل من أنتج مفتاحاً في أقل من 30 ثانية يملك الآن **إجابة سؤال»ما قيمة هذه الحماية؟»**."])


# ═══════════════════════════════ 11 ═══════════════════════════════
def ch11(d):
    d.new_chapter(11, "ما قد يُحرجك غداً: عيوب التطبيق التي رأيناها في اللقطات",
                  "ثلاثة أعطال من جذر واحد — وماذا تفعل إن لم تُصلَح قبل العرض")
    d.para("راجع `AUDIT_AND_FIXES.md` للتفصيل الكامل مع أرقام الأسطر. هنا فقط ما يخصّ **العرض**: ما سيحدث على المسرح إن تُركت الأخطاء كما هي.")
    d.h1("١١.١  الجذر الواحد: أحداث تُطلَق أثناء `InitializeComponent`")
    d.para("أي عنصر XAML يعطي قيمة ابتدائية **ويرتبط بمعالِج حدث** (`IsChecked`/`IsSelected`/`Text` + `Checked`/`SelectionChanged`/`TextChanged`) قد يعمل قبل أن تُولد بقية عناصر الصفحة ⇒ `NullReferenceException` داخل منشئ الصفحة.")
    d.table(["الموضع (كما في المستودع)", "ماذا يحدث", "ما يراه الجمهور"],
            [["`Pages/CustomersPage.xaml:23` — `CardsToggle IsChecked=\"True\" Checked=\"CardsToggle_Checked\"`", "المنشئ ينفجر ⇒ **نافذة «حدث خطأ غير متوقع»** التي في لقطة `215233.png`", "أسوأ مشهد ممكن: خطأ برمجي في أول نقرة"],
             ["`Pages/InvoicesPage.xaml:33-34` — `StatusCombo IsSelected=\"True\" SelectionChanged=\"Filter_Changed\"`", "`LoadAsync` يعمل قبل وجود `Loader`؛ الاستثناء يُعاد في `finally`، ويبقى `_loading = true` **للأبد** ⇒ كل `Refresh()` لاحق يُتجاهل بصمت", "جدول فواتير **فاضي** + «عرض 0-0 من 0» (لقطة 3)"],
             ["`Pages/ProductsPage.xaml:36-37` — نفس النمط على `CategoryCombo`", "نفس العطل ⇒ لا بطاقات، لا «لا توجد بيانات»، والفلاتر فارغة", "صفحة منتجات ميتة (لقطة 4)"],
             ["`Windows/NewInvoiceDialog.xaml:125-127` — `DiscountInput Text=\"0\" TextChanged=...` → `UpdateSummary()` يلمس `TotalText` (سطر 134)", "فتح «فاتورة جديدة» = انهيار آخر", "**لا تضغط هذا الزر أمام الطلاب** — هو الزر الذي يُفتح تلقائياً حين «تريد أن تريهم فاتورة»"]],
            widths=[62, 56, 55], align=["L", "L", "L"])
    d.callout("danger", "قرار العرض (اختر الآن)",
              ["**إما** ينفّذ الـ Agent الإصلاح (حرّاس `if (!IsInitialized) return;` + ضبط القيم الابتدائية من الكود + `finally` آمن) قبل أن تصوّر اللقطات؛ **أو** تعدّ «مسار عرض آمناً»: لوحة التحكم → الفواتير (ستبقى فاضية: لا تعد بالمعروض!) → التقارير → الإعدادات. ولا تفتح العملاء/المنتجات/فاتورة جديدة.",
               "ملاحظة دقيقة: الإصلاح يغيّر **لقطاتك** أيضاً. أعد التصوير بعد الإصلاح، بنفس الدقة، وإلا سيقارن أحدهم الصورة بالشاشة ويسألك عن الفرق."])
    d.h1("١١.٢  تراكب النصوص: `HorizontalAlignment=\"Left\"` في واجهة RTL")
    d.para("في WPF مع `FlowDirection=RightToLeft` تُعامَل `Left`/`Right` كـ Start/End **معكوسة**، فكل زر «جديد» وكل صندوق شهر/سنة وكل قيمة في قائمة حالة قفزت **فوق** العنوان (لقطات 2، 4، 5). العلاج: `Grid` بعمودين `*` و`Auto` بدل تراكم المحاذاة — تفصيله في `AUDIT_AND_FIXES.md` §ب/2.")
    d.h1("١١.٣  أشياء «ليست أعطالاً» لكنها ستُفهم غلطاً")
    d.bullets([
        "**«إجمالي الإيرادات» = فاتورة واحدة فقط**: الحساب صحيح (مُلخّص الشهر الحالي)، والتسمية هي المشكلة. قلها بصوت عالٍ قبل أن يسألها أحد: «هذا ملخّص الشهر، والبيانات مُبذورة حول تاريخ التشغيل».",
        "**«المصروفات ٠ ر.س»**: `DataSeeder` يولّد فواتير مبيعات فقط، فلا مشتريات — عيب بذور لا عيب محرّك؛ سيختفي إن أضفنا المشتريات (مهمة 5 في قائمة الـ Agent).",
        "**التاريخ «٢٦/٠٩/٢٠٢٦» في جدول الفواتير**: البذور نسبية لليوم ⇒ اللقطات القديمة تصبح مستقبلة. أعد التصوير.",
        "**`Select a date` بالإنجليزية داخل واجهة عربية** + أرقام عربية `٢٬٣٦٢٫١٠` مع معرّفات لاتينية `INV-2026-00012`: ثقافة غير مضبوطة؛ القرار في `AUDIT_AND_FIXES.md` §ب/3.",
        "**«٦ من V أيام» ليست `V`**: هي الرقم العربي `٧` في خط Windows — لا تقع في فخ تصحيحها على السبورة.",
        "**علامة «تنشيط Windows»**: في كل لقطة، وفي وجهك على المسرح. نشّط الجهاز أو استخدم VM نظيفة."])


# ═══════════════════════════════ 12 ═══════════════════════════════
def ch12(d):
    d.new_chapter(12, "الدفاع: كيف نحمي ميزان Pro فعلاً",
                  "من «كل ثغرة ← إصلاح» — وأنت تحتاج هذا الفصل لرفع الدرجة")
    d.para("العرض الذي يُظهر كسراً فقط يقول «نحن نعرف كيف نهاجم». العرض الذي **يُصلح** يقول «نحن مهندسون». الجدول أدناه مبني على الثغرات التي استغللناها نحن بالذات.")
    d.table(["ما استغللناه", "الإصلاح", "لماذا ينجع", "الكلفة"],
            [["مفتاح الترخيص يعمل على أي جهاز (لا يرتبط بشيء)", "توقيع رقمي: `ECDSA-P256` على `{key|HWID|expiry|plan}` بمفتاح خاص عند البائع، والتحقق بالمفتاح العام داخل البرنامج", "لا يمكن توليد مفتاح صالح بدون الخاص؛ والربط بالحهاز يوقف التسريب", "متوسطة (شهادة/خادم توقيع)"],
             ["القرار في دالة واحدة واضحة (`ParseAndVerify`)", "لا تُرجع `bool` — أعِد **مفتاح فك تشفير** للوظيفة (مثلاً مفتاح AES يُشتق من صحة الترخيص)، وافحص النجاح بفك تشفير فعلي", "الرتق «return true» لن يعيد شيئاً مفيداً؛ الكسر يحتاج استخراج شيء لا إرجاع قيمة", "عالية لكن فعّالة"],
             ["الحالة مخزَّنة في `HKCU` بنصّ قابل للتزوير", "`ProtectedData` (DPAPI, CurrentUser) + `HMAC` بمفتاح مشتق من البصمة + رقم تسلسلي مضاد للتراجع", "التزوير من خارج العملية يحتاج مفتاح المستخدم؛ الـ counter يمنع «إرجاع التاريخ»", "منخفضة"],
             ["`state.enc` بمفتاح AES داخل الملف", "المفتاح لا يكون في الحزمة: مشتق من الترخيص نفسه بعد التحقق منه عند الخادم، أو DPAPI", "«تشفير بمفتاح مضمّن» وهم أمني — أثبتناه في 6 خطوات", "منخفضة"],
             ["`InstallDate` كمرجع وحيد للتجربة", "سجّل `max(last_seen, now)` في مكانين (ملف + Registry) + `GetSystemTimePreciseAsFileTime` + monotonic ticks", "لا يمكن استعادة الأيام بتغيير التاريخ فقط", "منخفضة"],
             ["`DebugType=none` بلا تشويش، IL نظيف، حزمة بحجم معروف", "Obfuscation (Obfuscar/ConfuserEx) + **NativeAOT** أو `PublishReadyToRun` لإزالة IL", "لا يوجد `1F 49 D0` تبحث عنه؛ على المهاجم كود آلة معاد ترتيبه", "متوسطة — انظر ١٢.٢"],
             ["لا فحص تكامل ذاتي إطلاقاً", "عند الإقلاع: `SHA256` لملف `MizanPro.dll` (وليس لأمر IL) ومقارنة بقيمة موقعة؛ عند الاختلاف: لا تشغّل الوظيفة الحرجة", "رتق الملف يسقط فوراً؛ الرتق في الذاكرة يحتاج جهداً أعلى بكثير", "منخفضة"],
             ["لا يوجد كشف منقِّح", "`Debugger.IsAttached` + `CheckRemoteDebuggerPresent` + توقيت نداء قصير (rdtsc) — **كإشارة لا كقرار**", "يرفع الكلفة ويكشف التشغيل التلقائي؛ لا تعتمد عليه كـ«حماية»", "منخفضة + كاذبة أحياناً"]],
            widths=[36, 52, 50, 30], align=["L", "L", "L", "L"])
    d.h1("١٢.١  أهم جملة دفاعية تقال في هذا المشروع")
    d.para("الحماية الحقيقية **تُخرِج القرار من الجهاز**. لا يوجد طول مفتاح، ولا تشفير، ولا تشويش يجعل الترخيص المحلي «مكسوراً بصعوبة فقط»؛ الخطة التي تعمل: ترخيص قصير العمر من خادم (Token بصلاحية 24 ساعة) + صلاحية الوظائف تُقرأ من الخادم + تليمتري. عندها «كسر» التطبيق المحلي يعني فقط: نسخة تعمل يوماً واحداً بلا تحديث.")
    d.h1("١٢.٢  «ميزان Pro v2»: كيف نجعل ورشتنا نفسها أصعب (اقتراح للمستقبل)")
    d.code_block("""# 1) نقل القرار من bool إلى «مفتاح وظيفة»
// بدل: if (TokenProcessor.ParseAndVerify(key)) Activate();
byte[] feat = Crypto.DeriveFeatureKey(keyMaterial, machineFingerprint);

# 2) قفل IL بالترجمة المسبقة (يتطلب إعادة نشر)
dotnet publish -c Release -r win-x86 --self-contained true -p:PublishAot=true
#   ← لا MizanPro.dll ولا تيار #Text: ما تبقّى كود آلة فقط ← الفصل 7 كله يصعب

# 3) فحص تكامل عند أول نداء حسّاس (وليس عند الإقلاع فقط)
var h = Convert.ToHexString(SHA256.HashData(File.ReadAllBytes(assembly.Location)));
if (h != SignedHash) { /* عطّل الوظائف الحرجة، لا تُغلق بتلقائية */ }""",
                 title="ثلاث نقاط تُغلق الثغرات الثلاث الأكبر",
                 note="تحذير عملي: `PublishAot` يلغي `System.Management` (WMI) في .NET 6 — أي أن بصمة الجهاز يجب أن تُنقل إلى P/Invoke أو `GetAdaptersInfo`؛ لا تقترح Aot في التقرير قبل أن تعرف هذا.")
    d.callout("warn", "وإن أردت أن تبقى ورشة قابلة للحل",
              ["لا تجمع كل ما سبق؛ اختر **طبقتين** فقط (توقيع + مفتاح وظيفة). الهدف التعليمي أن يرى الطلاب «ماذا تغيّر ولماذا صار أصعب»، لا أن يقفوا أمام جدار. اقترح على المشرف نسخة «v2» كتحدي للموسم القادم — هذا أفضل ما يمكنك الخروج به من العرض."])


# ═══════════════════════════════ 13 ═══════════════════════════════
def ch13(d):
    d.new_chapter(13, "سيناريو العرض: 38 دقيقة، دقيقة بدقيقة",
                  "من يسكت ومتى، وما الذي تفعله إن سقط كل شيء")
    d.h1("١٣.١  التوزيع")
    d.table(["د", "المقطع", "من يتكلم", "على الشاشة", "الفعل الحاسم"],
            [["0–3", "المشكلة والهدف (ولماذا crackme وليس «برنامجاً حقيقياً»)", "علي", "شريحة العنوان + مخطط الطبقات", "اعرض `README.md` — لا تبدأ بالأدوات"],
             ["3–7", "كيف بُنيت الحماية: ثلاث طبقات + أرقام", "ماجد", "`TokenProcessor.cs` في VS Code", "اذكر 1314 = 73×18 بصوت عالٍ"],
             ["7–11", "تشريح IL + لماذا PublishSingleFile يفسد كل شيء", "المعتصم", "csproj + `dist\\` + dnSpy شجرة", "افتح `MizanPro.dll` (لا exe) أمامهم"],
             ["11–14", "كسر 1: تزوير `ActivationBlob` + `state.enc`", "ماجد", "cmd + python", "ثلاث لقطات: قبل/بعد/حذف القيمة"],
             ["14–24", "**كسر 2: رتق IL في الذاكرة بـ x32dbg**", "المعتصم (يحرّك) + علي (يشرح)", "x32dbg — لوحات CPU/Memory/Patches", "`F12` → `Ctrl+B` → `1F 49 D0` → `Ctrl+E` → `F9` → مفتاح الرتق"],
             ["24–28", "درس التوقيت (لماذا رفضت المرة الثانية؟) + JIT/tiering", "علي", "نفس الشاشة", "أعد التجربة بالمقلوب عمداً"],
             ["28–31", "كسر 3: `MessageBoxW` + call stack + `EAX`", "ماجد", "x32dbg Log", "سمِّه تشريحاً لا كسراً — كسب نقطة أمانة"],
             ["31–34", "الرتق الدائم (HxD) + مولّد مفاتيح حيّ", "المعتصم", "HxD + python", "ولّد مفتاحاً باسم طالب من الحضور"],
             ["34–37", "الدفاع: جدول «ثغرة ← إصلاح» + NativeAOT + التوقيع", "علي", "جدول الفصل 12", "اختم بـ: «لذلك نصبح مهندسين أفضل»"],
             ["37–38", "الأسئلة + كيف سيشرفون على مجموعات الطلاب", "الثلاثة", "شريحة الختام", "لا تزد — 38 دقيقة حدّك"]],
            widths=[10, 46, 26, 34, 58], align=["R", "L", "L", "L", "L"])
    d.h1("١٣.٢  البروفة المسائية (افعلها الليلة — 70 دقيقة)")
    d.steps([
        "ابنِ الحزمة: `dotnet publish -c Release -r win-x86 --self-contained true -p:PublishSingleFile=false -o dist` ← تحقق أن `MizanPro.dll` موجود.",
        "`reset-license.bat` → شغّل → سجّل دخول (admin/Admin@123) → لاحظ اللافتة «تجربة · ٦ أيام».",
        "الرتق المؤقت (الفصل 7) كاملاً، **بالمؤقّت**: 60–90 ثانية. سجّله.",
        "التجربة العكسية (المفتاح قبل الـ patch) — سجّلها؛ ستحتاجها غداً إن توترت يداك.",
        "الرتق الدائم عبر HxD على نسخة، واختبر `5NOFK-JQB1Z-7HSHF-NOP6D` بعد `reset-license.bat`.",
        "شغّل `keygen.py` واكتب مفتاحاً باسم كل طالب في الصف الأمامي (5 ثوانٍ لكل مفتاح).",
        "أعد كل شيء لنظافته: `reset-license.bat` + استبدل `MizanPro.dll` الأصلي + **أخرج من مجلد x32dbg**.",
        "صدّر فيديو البروفة (1080p) إلى `C:\\Lab\\backup\\demo.mp4` — هذا هو تأمينك."])
    d.callout("danger", "شجرة الطوارئ (احفظ الأرقام)",
              ["**A** — كل شيء يعمل: الفصل 7 ثم 9 ثم 10.",
               "**B** — النمط `1F 49 D0` لم يظهر في الذاكرة: انتقل فوراً لـ HxD (الفصل 9) وقل: «سنري الرتق على القرص لأنه أوضح»، وعُد للتوقيت في الشرح.",
               "**C** — dnSpy/HxD غير متاحين أو AV حذف الملفات: اعرض **keygen فقط** (الفصل 10) + مقطع الفيديو المسجّل + جدول الدفاع — وتكون قد غطيت 90% من الرسالة.",
               "**D** — المنقِّح نفسه رفض التشغيل على جهاز العرض: استخدم جهازك المحمول + وصّل HDMI، أو اعرض المقاطع المسجّلة وشرح حي على الشرائح. **لا تعتذر أكثر من 10 ثوانٍ**."])
    d.h1("١٣.٣  أنت تُشرف على 3 مجموعات — نظّم ذلك هكذا")
    d.bullets([
        "**قاعدة الأدوات**: مجموعة تستخدم dnSpy للـ IL ومجموعة تستخدم x32dbg للذاكرة ومجموعة للسلوك (Procmon/log BP). لن تتكرر العروض وستتعلم أنت الثلاثة.",
        "**نقطتا تحقّق فقط في البداية** (قبل أي كسر): «أين دالة القرار؟» و«ما الذي تثبته رسالتك النهائية؟». لا تسمح بأداة قبل هذين الجوابين.",
        "الأخطاء الثلاثة المتوقعة منهم: (1) يفتحون exe أحادي الملف ⇒ لا IL؛ (2) يفتّشون في `MizanPro.exe` عن `1F 49 D0` ⇒ لن يجدوا؛ (3) يحاولون `bp Namespace.Class.Method` ⇒ لا يعمل على المُدار. راجع معهم هذه الثلاثة خلال أول 10 دقائق.",
        "**سلمّ تقدير مختصر** (إن طُلب منك): 30% تحليل صحيح للدالة، 25% إثبات سببي (قبل/بعد)، 20% أداة مختارة بتبرير، 15% دفاع واقتراح إصلاح، 10% عرض ووضوح.",
        "امنح كل مجموعة 4 دقائق إلقائية + دقيقة واحدة «لو كنا مكانك لكسرنا…». أنت تعلّم، لا تُحبط."])


# ═══════════════════════════════ 14 ═══════════════════════════════
def ch14(d):
    d.new_chapter(14, "أسئلة ستُطرَح غداً — وإجاباتها جاهزة",
                  "14 سؤالاً: من المشرف، من الطلاب، ومن «اللي يعرف أكثر»")
    qa = [
        ("ليش x32dbg وليس x64dbg؟", "لأن الحزمة `win-x86` ⇒ العملية 32-بت، والـ debugger يجب أن يطابق عمارة الهدف. ولو نشروا `win-x64` لكان `x64dbg.exe`. (لو سُئلت «ليش ما نشرنا 64؟»: لأننا أردنا أن نُدرّس الفرق بين المعمارتين عملياً)."),
        ("ليش ما كسرت native مباشرة؟", "لأن منطق .NET يعيش في IL، وكود الآلة يولّده JIT عند الاستخدام وقد يعيد توليده (tiering). الرتق عند مصدر الحقيقة (IL) أصدق، وأسرع، وأقل هشاشة."),
        ("كيف عرفت أن `1F 49 D0` هو المكان الصحيح؟", "من dnSpy: `ldc.i4.s 73` + `rem` = `1F 49 D0`. تحققت في HxD على نفس الملف، ثم تأكدت أن البت التالي `2C` (`brfalse`) في الذاكرة."),
        ("ليش ما جعلت الدالة كلها `ret 1`؟", "فعلاً فعلناها (dnSpy: `ldc.i4.1; ret`) وهي أمتن لأنها لا تعتمد على ثوابت. لكن في الذاكرة نفضّل تغيير بايت واحد: لا يتغيّر طول الجسم، ولا يتحرك أي شيء، ولا يحتاج إعادة بناء."),
        ("هل هذا قانوني؟", "نكسر برنامجاً كتبناه لغرض تعليمي؛ لا توزيع، لا استخدام ضد الغير، ولا آثار على أنظمة حقيقية — والورشة كلها تحت إشراف أكاديمي. القانوني/AUP للمادة يُذكر في التقرير."),
        ("ما الفرق بين keygen و patcher؟", "الـ keygen **يُرضي** الشرط (لا يغيّر الهدف إطلاقاً)؛ الـ patcher **يُعطّل** الشرط. الأول أصعب رياضياً والأنظف، والثاني أصعب تقنياً وأسهل رياضياً."),
        ("ليش `sum % 73` وماشية؟", "لأنها «مُبهمة» لا «آمنة». القسمة على عدد أولي قرب 64 تجعل الشرط لا يبدو تافهاً، لكنها تحتفظ ببنية خطية (مجموع + XOR) يمكن حلّها — وهذا بالضبط درس «الغموض ≠ الأمان»."),
        ("ليش 73 بالذات؟", "لا سبب أمني. ملاحظة هندسية: القبول الفعلي هو `sum ∈ {1022,1168,1314,1460,1606,1752}` لأن XOR=0x0A يفرض مجموعاً زوجياً ⇒ 146 وليس 73."),
        ("ليش ما كشفك Antivirus؟", "لا شيء فعلناه يكتب على القرص في الفصل 7 (رتق في الذاكرة فقط). وفي الفصل 9 نحن من نسخنا ملفنا. برمجيات الكراك الحقيقية تُكتشف لأن سلوكها (حشو، حقن، تعديلات على ملفات الغير) نمطي."),
        ("لو أضفنا Strong Name؟", "سيُرفض تحميل الحزمة بعد رتق القرص (توقيع ≠ محتوى)، وسيسقط الفصل 9؛ أما الفصل 7 فلن يتأثر (نتعامل مع الصورة في الذاكرة). ولهذا نوصي بالتوقيع + فحص تكامل معاً."),
        ("ليش رتق الـ MessageBox ما يعتبر كسراً؟", "لأن `App.xaml.cs` يتجاهل نتيجة الصندوق؛ القرار اتُّخذ قبل أن يظهر. تعديل EAX يُغيّر العرض لا القرار — وشرحتُ ذلك للطلاب صراحة."),
        ("ليش لا تستخدم `bpdll` المكتوب في دليلكم؟", "لأن هذا الأمر غير موجود في x64dbg — صححناه إلى `bp user32.MessageBoxW` بعد System Breakpoint (أو «Break on DLL load»). كان يجب أن يُنقّح الدليل قبل الطباعة، وقد نقّحناه."),
        ("كيف تتأكد أن المفتاح الذي تولّدته «حقيقي» وليس صدفة؟", "بنفس الدالة: أعيد حساب `sum` و`xor` وأقارنهما بما يقرؤه الكود المصدري؛ ثم أجرّب المفتاح فعلياً في النسخة الأصلية قبل الرتق (يجب أن يُرفض) وبعده (يجب أن يُقبل)."),
        ("ما الذي لم تستطيعوا عمله؟ (الصراحة تُنقذك)", "لم نجرّب حماية خارجية حقيقية، ولا كشف التوقيع بـ anti-tamper وقت التشغيل، ولا unpacking لـ VMProtect/Themida. مقترحنا للفصل القادم: نسخة v2 بتوقيع + مفتاح وظيفة (الفصل 12).")]
    for q, a in qa:
        d.h3(q)
        d.para(a, size=10.0, space_after=3.2)


# ═══════════════════════════════ الملاحق ═══════════════════════════════
def appendices(d):
    d.new_chapter("A", "ملحق أ — مرجع x32dbg السريع", "اطبعه وضعه بجانب لوحة المفاتيح")
    d.h2("التنفيذ")
    d.table(["المفتاح", "ما يفعله", "المفتاح", "ما يفعله"],
            [["F9", "تشغيل / متابعة", "F12", "إيقاف مؤقت (pause)"],
             ["F7", "خطوة داخل (step in)", "F8", "خطوة فوق (step over)"],
             ["Ctrl+F9", "تنفيذ حتى العودة من الطريقة", "Alt+F9", "تشغيل حتى «كود المستخدم»"],
             ["F4", "تشغيل حتى السطر المحدد", "Ctrl+F2", "إعادة تشغيل الهدف"],
             ["Alt+F2", "إغلاق الهدف", "Alt+A", "الإلحاق بعملية جارية"]],
            widths=[20, 66, 20, 66], align=["L", "L", "L", "L"])
    d.h2("النقاط والبحث والتعديل")
    d.table(["الفعل", "مفتاح/أمر", "الفعل", "مفتاح/أمر"],
            [["نقطة توقف هنا", "F2 / `bp <addr>`", "مسح كل النقاط", "`bc`"],
             ["نقطة على تصحيح معماري / كتابة ذاكرة", "`bph` / `bpm`", "أمر يُنفَّذ عند الإصابة", "كليك يمين → Edit → Command"],
             ["ذهاب إلى عنوان أو اسم", "Ctrl+G", "بحث في الذاكرة", "Ctrl+F (نص) / Ctrl+B (بايتات)"],
             ["تحرير الذاكرة الثنائية", "Ctrl+E", "تجميع سطر (assemble)", "Space"],
             ["تعليق / تسمية", "`;` / `:`", "علامة مرجعية", "Ctrl+D"],
             ["عرض الرتقات", "Ctrl+P", "حفظ/تحميل تعليقاتنا", "`dbsave` / `dbload`"]],
            widths=[46, 40, 46, 40], align=["L", "L", "L", "L"])
    d.h2("اللوحات (View)")
    d.table(["الاختصار", "اللوحة", "نفعها في ورشتنا"],
            [["Alt+C", "CPU / Disassembly", "Space للتعديل، `;` للتعليق"],
             ["Alt+M", "خريطة الذاكرة", "بوابة العثور على `MizanPro.dll`"],
             ["Alt+K", "Call Stack", "إثبات أن النداء من `coreclr`/JIT"],
             ["Alt+B", "Breakpoints", "شروط، سجل، مرة واحدة، مسح"],
             ["Alt+L", "Log", "دليل «رأى / لم يرَ» أمام الجمهور"],
             ["Alt+T", "Threads", "لماذا CLR يضيف خيوطاً كثيرة"],
             ["Ctrl+P", "Patches", "كل ما رتّقناه في الجلسة"],
             ["Alt+N", "Notes", "ملاحظات تبقى مع `dbsave`"]],
            widths=[18, 54, 100], align=["L", "L", "L"])
    d.callout("note", "تذكير", ["بعض هذه الاختصارات قد يتغيّر بين إصدارات x64dbg — **جرّبها في البروفة** وثبّت الذي يعمل عندك؛ الدقة أهم من السرعة."])

    d.new_chapter("B", "ملحق ب — أوامر ومسارات جاهزة للنسخ", "كل ما ستكتبه غداً، مرة واحدة")
    d.code_block(""":: ============ البناء (مرة واحدة) ============
dotnet publish -c Release -r win-x86 --self-contained true -p:PublishSingleFile=false -o dist
dir dist\\MizanPro.dll          :: يجب أن يوجد — وإلا أوقف كل شيء

:: ============ البيئة (في نفس النافذة) ============
set DOTNET_TieredCompilation=0
set DOTNET_ReadyToRun=0

:: ============ حالات الترخيص ============
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob /f
reg delete "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate /f
del /f /q "%APPDATA%\\MizanPro\\state.enc"
reg add   "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob /t REG_SZ /d M1Z4N-2025P-ROM4S-T3R01 /f
reg add   "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v InstallDate     /t REG_SZ /d 20250101 /f
reg query "HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState" /v ActivationBlob

:: ============ داخل x32dbg ============
bp user32.MessageBoxW            :: ثم: كليك يمين → Log text = {p:arg1} → Break Never → Command = gc
bp user32.MessageBoxIndirectW    :: وقاية (بعض الرسائل تمر من هنا)
bp advapi32.RegSetValueExW       :: إثبات «متى يكتب التطبيق؟»
Ctrl+B → 1F 49 D0                :: ابحث في MizanPro.dll
Ctrl+E → 49 إلى 01               :: الرتق
set eax, 0                       :: تمرين قيمة الإرجاع (ليس كسراً)

:: ============ الملفات ============
C:\\Lab\\keygen.py        :: الفصل 10
C:\\Lab\\forge_state.py   :: الفصل 6
C:\\Lab\\reset-license.bat:: الفصل 3.4
C:\\Lab\\backup\\          :: MizanPro.dll.orig + MizanPro_patched.dll""",
                 title="CMD / x32dbg — انسخ والصق",
                 note="الأسماء العربية في المسارات (`Desktop\\سنه4\\...`) تسبب مشاكل لبعض الأدوات عند الاستخراج؛ استخدم `C:\\Lab\\` كما هنا.")

    d.new_chapter("C", "ملحق ج — الأرقام والمفاتيح (مُتحقَّق منها)", "اطبعها واحتفظ بها في جيبك")
    d.table(["البند", "القيمة", "من أين"],
            [["طول المفتاح", "20 محرفاً (الشرطات تجميلية)", "TokenProcessor.cs:67"],
             ["الأبجدية", "`[A-Z0-9]`، حالة الأحرف لا تهم", "TokenProcessor.cs:68-73"],
             ["الشرط 1", "`Σ ASCII % 73 == 0`", "TokenProcessor.cs:89"],
             ["الشرط 2", "`XOR == 0x0A` (`const byte` مضمَّن في IL)", "TokenProcessor.cs:89 و94"],
             ["قاعدة مستنتجة", "المجموع مضاعف **146** لا 73 (زوجية)", "برهان 10.1"],
             ["مفاتيح صالحة", "`M1Z4N-2025P-ROM4S-T3R01` · `AAAAABBBBBCCCCCADN89` · `MIZAN-2026X-32DBG-B36SQ` · `CRACK-MEWOR-KSHOP-BY0A5` · `MIZAN-PROCR-ACKED-P4P7L`", "فحص عددي (10.3)"],
             ["مفتاح «الرتق فقط»", "`5NOFK-JQB1Z-7HSHF-NOP6D` (Σ=1426، 1426%73=39، XOR=0x0A)", "يُرفض قبل الرتق، يُقبل بعده"],
             ["مفتاح مرفوض موثّق", "`AAAAA-BBBBB-CCCCC-DDDDD` (Σ=1330، %73=16، XOR=0x04)", "مثال الفشل في المسودات"],
             ["نمط الرتق", "`1F 49 D0` ← `1F 01 D0` (بايت واحد)", "§2.2، §7.2"],
             ["بدائل الرتق", "`2C xx`←`90 90` (فرع المجموع) · كلا الفرعين ⇒ أي مفتاح 20 محرفاً", "§7.4"],
             ["مسار الحالة", "`HKCU\\Software\\MizanSolutions\\Mizan\\RuntimeState` (`ActivationBlob`, `InstallDate`)", "ProductStateEngine.cs:47-49"],
             ["ملف الحالة", "`%AppData%\\MizanPro\\state.enc` = Base64(AES-128-CBC(`MIZAN_VALID|<بصمة>`))", "ProductStateEngine.cs:103-124, 168-171"],
             ["AES", "key `Mizan2024_K3y128` · IV `MizanPro_IV_2024` · PKCS7", "ProductStateEngine.cs:59-62"],
             ["البصمة", "SHA256(`MachineName|UserName|MAC`)[0:8] → XXXX-XXXX-XXXX-XXXX", "TokenProcessor.cs:106-117"],
             ["الدخول", "`admin` / `Admin@123` · `accountant` / `Acc@2024`", "Data/DataSeeder.cs"],
             ["فترة التجربة", "7 أيام، `TrialPeriodDays` تُقرأ عند الإقلاع فقط", "ProductStateEngine.cs:127-148"]],
            widths=[30, 108, 34], align=["R", "L", "L"])

    d.new_chapter("D", "ملحق د — قاموس المصطلحات (عربي/إنجليزي)", "استخدم نفس اللفظ طوال العرض")
    d.table(["الإنجليزي", "العربي", "المعنى في ورشتنا"],
            [["crackme", "برنامج كسر تعليمي", "هدفنا MizanPro"],
             ["keygen", "مولّد مفاتيح", "يشتق مفتاحاً من نفس الرياضيات"],
             ["patch / patcher", "رتق / مُرتِّق", "بايت واحد يغيّر القرار"],
             ["IL (Intermediate Language)", "لغة الوسيط", "ما تكتبه dnSpy وما نرتقه"],
             ["JIT", "الترجمة اللحظية", "تُفسّر لماذا التوقيت يحكم"],
             ["tiered compilation", "الترجمة متعددة المستويات", "`tier0` سريع ثم `tier1` مُحسَّن"],
             ["ReadyToRun / NativeAOT", "ترجمة مسبقة", "تجعل IL غير موجود"],
             ["apphost", "قشرة التشغيل", "`MizanPro.exe` الذي يشغّل DLL"],
             ["single-file bundle", "حزمة أحادية الملف", "سبب فشل dnSpy عندنا"],
             ["memory map", "خريطة الذاكرة", "Alt+M — باب البحث"],
             ["binary pattern", "نمط ثنائي", "`1F 49 D0`"],
             ["breakpoint (software)", "نقطة توقف برمجية", "`int3`/`CC`"],
             ["conditional / log-only BP", "نقطة مشروطة / سجلية", "الأناقة في الفصل 8"],
             ["execute till return", "تنفيذ حتى العودة", "Ctrl+F9 — لالتقاط EAX"],
             ["call stack", "مكدّس النداءات", "Alt+K — إثبات «من نداء؟»"],
             ["HWID / machine fingerprint", "بصمة الجهاز", "تربط state.enc بالحهاز"],
             ["activation blob", "كتلة التفعيل", "اسم القيمة في Registry"],
             ["trial period", "فترة التجربة", "7 أيام"],
             ["nag screen", "شاشة الإلحاح", "صندوق «انتهت التجربة»"],
             ["integrity check", "فحص التكامل", "ما لا يملكه MizanPro"],
             ["strong name / signature", "توقيع الحزمة", "غيابه سمح رتق القرص"],
             ["obfuscation", "تشويش/تمويه", "رفع كلفة، لا منع"],
             ["DPAPI / ProtectedData", "تشفير بمفتاح المستخدم", "الإصلاح الرخيص الأول"],
             ["HMAC", "رمز مصادقة بمفتاح", "يمنع تزوير الحالة"],
             ["first-chance exception", "استثناء الفرصة الأولى", "`0xE0434352` الذي يجب تجاهله"]],
            widths=[42, 42, 88], align=["L", "R", "L"])

    d.new_chapter("E", "ملحق هـ — ما تسلّمه، ومن أينقرأون", "قائمة التسليم + مراجع لا تُتهم فيها بالتلفيق")
    d.h2("ملفات التسليم (نسخة ورشة كاملة)")
    d.bullets([
        "`AUDIT_AND_FIXES.md` — التقرير التحليلي للصور والكود ومهام الـ Agent.",
        "`Crack_Guide_x32dbg.pdf` — هذا الدليل (يُوزَّع على الطلاب قبل الورشة).",
        "`MizanPro_Crack_Workshop.pptx` — شرائح العرض (38 دقيقة) مع ملاحظات المتحدث.",
        "`LabSheet.md` (بعد تصحيح التلميح الرياضي — §10.٤) — ورقة عمل الطلاب.",
        "`Workshop_Pack.bat` المُحدَّث — يجمع `dist\\` كاملاً + الأدوات + الـ PDF.",
        "`tools/keygen.py`، `tools/forge_state.py`، `tools/reset-license.bat`.",
        "`backup/demo.mp4` — البروفة المسجّلة (تُذكر في التقرير كجزء «قابلية التكرار»).",
        "تقرير قصير (4–6 صفحات): الهدف، المنهجية، النتائج، **الدفاع**، والإخفاقات بصدق — هذا الجزء يقرأه المشرف فعلاً."])
    d.h2("مصادر تُذكر باسمها (لا تختلق روابط)")
    d.bullets([
        "توثيق **x64dbg** الرسمي (x64dbg.com/docs): الاختصارات، الأوامر `bp/bc/bph/bpm/log/msg/find/dbsave`، وخيارات Events/Exceptions — كل أمر في هذا الدليل طابقناه معه.",
        "وثائق مايكروسوفت: «Single file deployment» و«Tiered compilation» و«Runtime configuration environment variables» (`DOTNET_*`) — للأسباب في §2.٤.",
        "مستودعات الأدوات: **dnSpyEx** ( فرع الصيانة النشط لمشروع dnSpy الأصلي)، **HxD**، **PE-bear**، **Sysinternals Procmon** — نزّلها من مستودعاتها الرسمية، وحفظ نسخة في `tools\\` للورشة.",
        "مراجع المفاهيم العامة: فصل «Software Cracking» في موسوعات الهندسة العكسية، وكتب *Reversing* (E. Salmony) و*Practical Malware Analysis* (لأسلوب التحليل السلوكي الذي سرناه في الفصل 8).",
        "في داخلي المشروع: `README.md` (تصميم الحماية)، `CrackGuide.md` (سيناريو المشرف — بعد تصحيحاته)، `.github/workflows/build.yml` (ما تغطيه CI وما لا تغطيه — §11.٤ من تقرير التدقيق)."])
    d.callout("ok", "قبل أن تصعد",
              ["المفاتيح الخمسة مكتوبة في جيبك؟ · `dist\\MizanPro.dll` موجود؟ · البروفة المسجّلة جاهزة؟ · AV معطّل مؤقتاً على جهاز العرض؟ · `reset-license.bat` يعمل؟ · **لا تضغط «فاتورة جديدة»**؟",
               "بالتوفيق — وإن سقط شيء، فالحل في الفصل 13.٢/«شجرة الطوارئ». لا يوجد عرض ناجح بلا خطة بديلة."])
