# ورشة كسر الحماية — ميزان Pro

## دليل المقدِّم خطوة بخطوة

| | |
|---|---|
| **الفريق** | علي فيصل العطيفي · ماجد الدميني · المعتصم بالله الراعي |
| **المشرف** | م. عبدالكريم الباشيري |
| **المدة** | 35–40 دقيقة (+ 5 دقائق إشراف متداخلة مع التجربة الأخيرة) |
| **الهدف** | تحليل ثلاث طبقات حماية (Registry ← ملف مشفّر ← خوارزمية مفتاح) وكسرها عملياً |

---

## ⚠️ ثلاث ملاحظات خبير — اقرأها قبل المحاضرة

هذه الملاحظات تصحّح الخطة الأولية للورشة حتى تعمل **أمام الطلاب بدون مفاجآت**.
اقرأها أولاً، ثم اشرحها للطلاب في وقتها — كل واحدة منها درس بحد ذاته:

1. **قيمة XOR هي `0x0A` وليس `0x2A`:** الشرط الأصلي في المسودة (`xor == 0x2A`)
   **مستحيل رياضياً** لأي مفتاح من 20 محرفاً من `[A-Z0-9]` — برهان البتّات:
   الأرقام `0x30-0x39` (بت6=0، بت5=1) والأحرف `0x41-0x5A` (بت6=1، بت5=0)،
   ومع 20 محرفاً يجب أن يكون بت6 وبت5 في XOR النهائي **بنفس الزوجية**،
   بينما `0x2A` يفرض بت6=0 وبت5=1 معاً — تناقض. النسخة المشحونة تستخدم `0x0A`
   (أصغر تصحيح ممكن). لو شغّلت سكربت Python بالقيمة `0x2A` فسيدور **إلى ما لا نهاية**.

2. **المفتاح الخاطئ لا يُظهر MessageBox:** واجهة التفعيل تعرض الخطأ **داخل الصفحة**
   (نص أحمر inline) وليس في نافذة حوار — لذلك نستخدم في المرحلة 3 رسالة
   «انتهت الفترة التجريبية» (وهي MessageBox حقيقي) كمشغّل لنقطة التوقف —
   وهي أصلاً أكثر ارتباطاً بالترخيص وأكثر دراماتيكية.

3. **المفتاح الخاطئ لا يكتب شيئاً في القرص:** التحقق فشل قبل أي كتابة — لذلك
   نعرض في المرحلة 1 عمليات الكتابة بعد تفعيل **ناجح** بمفتاح صحيح معروف مسبقاً.

**مفاتيح صحيحة جاهزة للتحقق (مولّدة مسبقاً بالخوارزمية الفعلية):**

| المفتاح | الاستخدام في الورشة |
|---|---|
| `M1Z4N-2025P-ROM4S-T3R01` | التفعيل الناجح في المرحلة 1 |
| `AAAAABBBBBCCCCCADN89` | مفتاح احتياطي |

**قائمة التجهيز المسبق** (قبل بدء المحاضرة على جهاز العرض):
- [ ] MizanPro.exe يعمل ويقبل تسجيل دخول `admin / Admin@123`
- [ ] Procmon + dnSpy + x32dbg + Python مثبتة وتفتح بلا UTX رسائل
- [ ] التجربة سارية (عدّل `InstallDate` في الـ Registry إلى تاريخ اليوم عند الحاجة)
- [ ] نسخة احتياطية نظيفة من MizanPro.exe (بعض التجارب تعدّل الملف)
- [ ] هذا الدليل + LabSheet.md مطبوعان/مشاركان مع الطلاب

---

## جدول الورشة

| المرحلة | الأداة | الزمن |
|---|---|---|
| 0 — استكشاف الهدف | التطبيق | 3 دق |
| 1 — أين يسكن الترخيص؟ | Procmon | 7 دق |
| 2 — إيجاد الخوارزمية | dnSpy | 10 دق |
| 3 — الكسر المؤقت | x32dbg | 8 دق |
| 4 — الكسر الدائم | dnSpy | 7 دق |
| 5 — الكسر الذكي | Python | 5 دق |
| إشراف الطلاب | — | متداخل مع نهاية المرحلة 5 |

---

## المرحلة 0 — استكشاف الهدف (3 دق)

- شغّل التطبيق + سجّل الدخول بـ `admin / Admin@123`
- افتح **الإعدادات ← الترخيص** ولاحظ شريط التجربة (7 أيام) و**معرّف الجهاز**
- اخرج، ثم من نافذة الدخول اضغط «تفعيل المنتج» وجرّب مفتاحاً عشوائياً
  مثل `AAAAA-BBBBB-CCCCC-DDDDD` → «مفتاح الترخيص غير صالح، تحقق من المفتاح وأعد المحاولة»
- **سؤال للطلاب:** «أين يُخزَّن هذا التحقق؟ هل هو في ملف؟ في الـ Registry؟ في الذاكرة فقط؟»
- [ملاحظة: لا تجب — دعهم يخمّنون. اكتب التخمينات على السبورة وعُد إليها بعد المرحلة 1]

---

## المرحلة 1 — Procmon: أين يُخزَّن الترخيص؟ (7 دق)

1. شغّل **Procmon كـ Administrator**
2. اضبط الفلتر:
   - `Process Name` → `is` → `MizanPro.exe`
   - `Operation` → `is` → `RegSetValue` **و** `WriteFile` (فلتران)
3. **التجربة أ (فشل):** أعد محاولة التفعيل بمفتاح خاطئ → لاحظ: **لا عمليات كتابة إطلاقاً**
   - الدرس: «التحقق فشل قبل الوصول لأي تخزين — الخطأ ظهر في الواجهة فقط»
4. **التجربة ب (نجاح):** فعّل بالمفتاح الصحيح `M1Z4N-2025P-ROM4S-T3R01` →
   Procmon يعرض فوراً:
   - `RegSetValue` على `HKCU\Software\MizanSolutions\Mizan\RuntimeState\ActivationBlob`
   - `WriteFile` على `%APPDATA%\MizanPro\state.enc`
5. افتح regedit و`%APPDATA%\MizanPro` وأرِ الطلاب القيمتين فعلياً
6. أعد الظروف الأصلية للورشة: احذف `ActivationBlob` و`InstallDate` من الـ Registry
   واحذف `state.enc` (أو شغّل أداة تنظيف) — ثم أعد تشغيل التطبيق ليعود لوضع التجربة

**الدرس:** «الآن نعرف أين يُخزَّن الترخيص بدون قراءة سطر كود واحد —
مراقبة السلوك (Behavioral Analysis) تسبق فك التجميعة دائماً»

---

## المرحلة 2 — dnSpy: البحث الصحيح (10 دق)

1. افتح **dnSpy** واسحب `MizanPro.exe` داخل نافذته (File ← Open)
2. **الخطأ الشائع (أرضِه للطلاب عمداً):** تصفّح شجرة الأسماء يدوياً بحثاً عن
   `License` أو `Protection` → **لا يجد شيئاً** — لأن الأسماء مضلِّلة عمداً
   (`TokenProcessor`؟ اسم بريء تماماً!)
3. **الطريقة الصحيحة:** `Edit → Search Assemblies (Ctrl+Shift+K)` ←
   غيّر نوع البحث إلى **String/Search (Text)** ← اكتب: `غير صالح`
4. النتيجة الوحيدة تقود إلى `ProductStateEngine.CommitActivation()` —
   وهي التي أعادت رسالة الخطأ التي رأيناها في المرحلة 0
5. **تتبّع سلسلة الاستدعاء نزولاً:**
   `CommitActivation()` ← تستدعي ← `TokenProcessor.ParseAndVerify(string)`
6. اقرأ وفسّر الكود مع الطلاب سطراً سطراً:

```csharp
string clean = rawToken.Replace("-", "").ToUpper();   // تنظيف الشرطات
if (clean.Length != 20) return false;                 // الطول بالضبط
// ... فحص الأبجدية [A-Z0-9] ...
int sum = clean.Sum(c => (int)c);                     // مجموع الأكواد
byte xor = 0; foreach (char c in clean) xor ^= (byte)c;
return sum % 73 == 0 && xor == RequiredXor;           // RequiredXor = 0x0A
```

7. **صندوق النقاش — قصة 0x2A (استخدمها لتلميع الفريق):**
   «في مسودة التصميم كان الشرط `xor == 0x2A` — أثبت الفريق رياضياً استحالته
   (برهان زوجية البتّين 5 و6) قبل اعتماد `0x0A`. من يشرح لماذا لا يمكن لأي مفتاح
   تحقيق 0x2A؟» — تلميح: اكتب على السبورة أكواد ASCII بالثنائي.

**الدرس:** «الخوارزمية كلها أمامنا الآن — .NET يحتفظ بكل شيء: الأسماء، النصوص، المنطق»

---

## المرحلة 3 — x32dbg: الكسر المؤقت (8 دق)

> الهدف: فهم نقاط التوقف على مستوى الأكواد الآلية وقيمة الإرجاع EAX —
> ولماذا هذا المستوى صعب مع .NET (جسر للمرحلة 4).

1. **التجهيز:** أغلق التطبيق ← في regedit عدّل `InstallDate` (المسار من المرحلة 1)
   إلى `20250101` ← الآن التطبيق سيرفض العمل برسالة «انتهت الفترة التجريبية»
2. افتح **x32dbg** ← `File → Open` ← `MizanPro.exe` (ملف x86 — لذلك x32dbg وليس x64)
3. في سطر الأوامر الأسفل اكتب: `bpdll user32.dll` ثم `F9`
   — التوقف عند تحميل user32 (نحتاجه لأن MessageBoxW لن يُحلَّل قبل تحميلها)
4. اكتب: `bp MessageBoxW` ثم `F9`
5. **اللحظة:** التطبيق يعرض «انتهت الفترة التجريبية المجانية…» ويتوقف x32dbg
   **داخل MessageBoxW** قبل ظهور الرسالة
6. `View → Call Stack` — أرِ الطلاب الإطارات: نزول عبر `clr.dll` وذاكرة JIT —
   «القرار اتُّخذ في كود مُدار مُترجَم لحظياً — لا أسماء ولا رموز هنا»
7. **لحظة EAX:** اضغط `Ctrl+F9` (Execute till Return) → تظهر الرسالة، اضغط موافق →
   يتوقف x32dbg عند الإرجاع و**EAX = 1** (IDOK) — «هذه قيمة إرجاع الرسالة،
   وليست نتيجة التحقق — القرار اتخذ قبلها»
8. غيّر EAX يدوياً إلى `0` (كليك على القيمة في نافذة السجلات ← Modify)
   ثم `F9` → التطبيق يغلق كالمعتاد — «عدّلنا القيمة الخطأ، والأثر مؤقت
   ينتهي بإغلاق التطبيق مهما عدّلنا»

**الدرس:** «هذا النوع من الكسر مؤقت فقط — ينتهي عند إغلاق التطبيق،
وفي .NET نقطة القرار الحقيقية تعيش في IL — هذا موضوع المرحلة التالية»

> [ملاحظة: إن تعذّر تحميل user32 كما هو مخطط، البديل السريع: شغّل التطبيق
> بشكل طبيعي، سجّل الدخول، افتح صفحة الفواتير واضغط زر 👁 (عرض التفاصيل) —
> بعد ضبط `bp MessageBoxW` — وستحصل على نفس نقطة التوقف]

---

## المرحلة 4 — dnSpy: الكسر الدائم (7 دق)

1. ارجع إلى dnSpy ← `TokenProcessor.ParseAndVerify()`
2. كليك يمين على الدالة ← **Edit Method (C#)**
3. التعديل:

```csharp
// قبل:
return sum % 73 == 0 && xor == RequiredXor;

// بعد:
return true;
```

4. `Compile` ← `File → Save Module` ← احفظ باسم `MizanPro_Cracked.exe`
5. شغّل النسخة المعدّلة ← التفعيل ← أدخل `AAAAA-BBBBB-CCCCC-DDDDD` →
   **«تم تفعيل ميزان Pro بنجاح!»** 🎉
6. افتح regedit وأرِ الطلاب أن `ActivationBlob` كُتب فيه المفتاح العشوائي!

**الدرسان:**
- «كُسر دائم: التعديل محفوظ في الملف نفسه»
- **سؤال تحدٍ للختام:** «شغّل الآن النسخة الأصلية (غير المعدّلة) — ماذا يحدث
  للـ ActivationBlob المخزّن؟ ولماذا يظل التطبيق يعمل رغم ذلك؟»
  *(الجواب للمقدِّم: النسخة الأصلية تكتشف أن المفتاح غير صالح → تحذف القيمة،
  لكن المسار الاحتياطي state.enc ما زال صالحاً على هذا الجهاز فيسمح بالتشغيل —
  نقاش رائع عن طبقات الحماية!)*

---

## المرحلة 5 — Python: الكسر الذكي (5 دق)

مولّد مفاتيح يحقق الشرطَين **حقيقياً** — يعمل على النسخة **الأصلية** غير المعدّلة:

```python
import random, string

def gen():
    c = string.ascii_uppercase + string.digits
    while True:
        r = ''.join(random.choices(c, k=20))
        s = sum(ord(x) for x in r)
        x = 0
        for ch in r: x ^= ord(ch)
        # ⚠️ 0x0A وليس 0x2A — 0x2A مستحيلة رياضياً (انظر المرحلة 2)
        if s % 73 == 0 and x == 0x0A:
            return f"{r[:5]}-{r[5:10]}-{r[10:15]}-{r[15:]}"

print(gen())
```

- شغّله أمام الطلاب (يعطي نتيجة خلال أجزاء من الثانية) وادع طالباً لتشغيله مرة أخرى
- أدخل المفتاح المولَّد في **التطبيق الأصلي** ← «تم تفعيل ميزان Pro بنجاح!»
- ذكّرهم بالتحقق المزدوج: المفتاح `M1Z4N-2025P-ROM4S-T3R01` يمرر نفس الدالة

**الدرس:** «فهم الخوارزمية = توليد مفاتيح لا نهائية — لا حاجة لتعديل البرنامج إطلاقاً.
هذا الفرق الجوهري بين Patch و Keygen: الأول يترك أثراً في الملف، والثاني لا يمسّه»

---

## الخلاصة التعليمية (2 دق داخل المرحلة 5)

- **لماذا .NET سهل التحليل؟** IL مقروء بدون تشفير + الأسماء والنصوص محفوظة في
  الـ Metadata + أدوات ناضجة (dnSpy/ILSpy)
- **كيف تحمي تطبيقك؟**
  1. تعمية وخلط: ConfuserEx / .NET Reactor / Obfuscar
  2. تحقق على الخادم (Server-side) — لا تثق بجهاز المستخدم أبداً
  3. Anti-tamper: فحص تكامل التجميعة ذاتياً وربط النتيجة بمنطق البرنامج
  4. مفاتيح موقّعة رقمياً (RSA) بدل خوارزمية حسابية قابلة للاشتقاق

---

## إشراف الطلاب (آخر 5 دقائق — متداخلة)

كل طالب على جهازه ينفّذ **المرحلة 4** بنفسه (البسيطة والأكثر إبهاراً):
1. فتح MizanPro.exe في dnSpy
2. الوصول إلى `ParseAndVerify` عبر البحث النصي عن `غير صالح`
3. `Edit Method` ← `return true;` ← `Save Module`
4. تفعيل بأي مفتاح ← لقطة شاشة للنجاح (تُسلَّم ضمن تقرير LabSheet)

**للمتفوقين:** تطبيق المرحلة 5 كاملة (keygen يعمل على النسخة الأصلية).

---

## استكشاف أخطاء سريع (للمقدِّم)

| المشكلة | الحل |
|---|---|
| dnSpy لا يفتح الملف | تأكد أنه MizanPro.exe وليس نسخة _Cracked التالفة — أعد النسخة الاحتياطية |
| bp MessageBoxW لا يُعثَر عليه | user32 لم تُحمَّل بعد — نفّذ `bpdll user32.dll` ثم أعد الضبط |
| التطبيق «مفعّل» فجأة على جهاز العرض | امسح `ActivationBlob`+`InstallDate` و`state.enc` (مسارات المرحلة 1) |
| Python يدور بلا نتيجة | القيمة `0x2A` وليس `0x0A` — راجع صندوق الاستحالة في المرحلة 2 |
| شاشة التفعيل ترفض 21+ محرفاً | حقل الإدخال يقبل 24 محرفاً بما فيها الشرطات — الصق المفتاح كاملاً |
