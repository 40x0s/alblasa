# ورشة عملية — تحليل وكسر حماية ميزان Pro

**الفريق:** علي فيصل العطيفي · ماجد الدميني · المعتصم بالله الراعي
**المدة المقترحة:** 90 دقيقة · **البيئة:** Windows 10/11 + الأدوات المذكورة في `Tools_Links.txt`

> الهدف: فهم طبقات الحماية الثلاث في التطبيق (Registry → ملف مشفّر → خوارزمية مفتاح)
> وتحليلها عملياً، ثم مناقشة كيف تُبنى حماية أقوى.

**بيانات الدخول:** `admin / Admin@123`

---

## التجربة 0 — التشغيل والاستكشاف (10 دقائق)

☐ شغّل `MizanPro.exe` وسجّل الدخول بحساب المدير
☐ افتح **الإعدادات ← تبويب الترخيص** ولاحظ: حالة التجربة (عدد الأيام) + **معرّف الجهاز**
☐ اضغط "نسخ" بجانب معرّف الجهاز واحفظه — ستحتاجه في التجربة 5

**أسئلة:**
- كم يوم تجربة متبقٍ؟
- بصمة الجهاز مبنية من أي عناصر برأيك؟ (سنتحقق في التجربة 2)

---

## التجربة 1 — Process Monitor: أين تُخزَّن الحالة؟ (15 دقيقة)

☐ شغّل **Procmon** ثم اضبط الفلاتر:
   - Process Name → is → `MizanPro.exe`
   - Operation → begins with → `Reg`
☐ أعد تشغيل `MizanPro.exe` وراقب عمليات القراءة/الكتابة
☐ حدّد المسار الكامل للمفتاح الذي يقرأ منه التطبيق حالة التشغيل

**المتوقع أن تجد (تحت `HKCU`):**
- `...\RuntimeState` وفيه القيمة `InstallDate` بصيغة `yyyyMMdd`
- `...\RuntimeState` وفيها `ActivationBlob` بعد التفعيل

☐ افتح `regedit` وعدّل `InstallDate` إلى تاريخ قديم ثم أعد التشغيل — ماذا يحدث للتجربة؟
☐ **سؤال تحدٍ:** لماذا لا تكفي إعادة ضبط `InstallDate` وحدها لكسر الحماية؟
*(تلميح: هل يوجد مسار احتياطي آخر يسمح بالتشغيل؟)*

---

## التجربة 2 — dnSpy: استخراج الخوارزمية (20 دقيقة)

☐ افتح `MizanPro.exe` في **dnSpy**
☐ اضغط `Ctrl+Shift+K` (Search) → اختر بحث في النصوص وابحث عن: `غير صالح`
☐ انتقل إلى الدالة التي تحتوي النص ← هي رسالة الفشل في `CommitActivation`
☐ تتبّع سلسلة الاستدعاء صعوداً حتى تصل إلى: `TokenProcessor.ParseAndVerify()`

**المطلوب استخراجه من الكود (سجلّه في تقريرك):**

| العنصر | القيمة المستخرجة |
|---|---|
| طول المفتاح بعد تنظيف الشرطات | ؟ |
| الأبجدية المسموحة | ؟ |
| شرط المجموع (sum) | sum % ؟ == 0 |
| شرط الإقصاء (XOR) | xor == 0x؟؟ |
| القيمة المطلوبة (Hex) | ؟ |

☐ انظر أيضاً إلى `ComputeMachineFingerprint()` — من أي عناصر تُبنى بصمة الجهاز؟

**سؤال تحدٍ (نقاش جماعي):**
في المسودة الأولى للمشروع كان الشرط `xor == 0x2A` — أثبتنا أنه **مستحيل رياضياً**
لأي مفتاح من هذه الأبجدية بهذا الطول. هل تستطيع تفسير السبب؟
*(تلميح: اكتب أكواد ASCII للأحرف وال أرقام بالنظام الثنائي وقارن البتّين 5 و 6.)*

---

## التجربة 3 — التعديل المباشر (Patch) (15 دقيقة)

☐ في dnSpy: كليك يمين على `ParseAndVerify` ← **Edit Method (C#)**
☐ استبدل جسم الدالة بسطر واحد: `return true;` ثم اضغط Compile
☐ احفظ التعديل: File ← Save Module (باسم `MizanPro-Patched.exe`)
☐ شغّل النسخة المعدّلة، اذهب إلى التفعيل وأدخل أي مفتاح عشوائي ← ماذا يظهر؟

**أسئلة:**
- هل أصبحت الحالة `PRO`؟ راجع بطاقة الترخيص في الإعدادات
- ماذا يحدث لقيمة `ActivationBlob` في الـ Registry؟ (راجع Procmon)
- لماذا يُعدّ هذا الأسلوب "كسراً هشاً"؟ (فكّر: تحديثات البرنامج، فحص التكامل الذاتي)

---

## التجربة 4 — كتابة Keygen حقيقي (25 دقيقة)

المطلوب: سكربت Python يولّد مفتاحاً **صالحاً** (بدون تعديل البرنامج) باستخدام
الخوارزمية التي استخرجتها في التجربة 2.

**الخوارزمية (بعد الاستخراج):**
1. المفتاح = 20 محرفاً من `[A-Z0-9]` (الشرطات تجميلية)
2. `sum(codes) % 73 == 0`
3. `XOR(codes) == <القيمة التي استخرجتها>`

**تلميح الحل:** ثبّت أول 18 محرفاً بأي صيغة تريدها، ثم جرّب كل combinations
من محرفين (36 × 36 = 1296 احتمالاً فقط) واختبر الشرطين — ستجد عادة عدة حلول.

```python
# هيكل بداية — أكمل الباقي
ALPHABET = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"

def check(key: str) -> bool:
    clean = key.replace("-", "")
    if len(clean) != 20: return False
    # ... أكمل: شرط الأبجدية + شرط المجموع + شرط XOR (من التجربة 2)

def keygen() -> str:
    # ... ثبّت 18 محرفاً ثم ابحث عن محرفين يحققان الشرطين
    ...

if __name__ == "__main__":
    key = keygen()
    print(key, "->", check(key))
```

**للتحقق من سكربتك:** هذا مفتاح صحيح معروف يجب أن يجتاز دالة `check`:
`M1Z4N-2025P-ROM4S-T3R01`

☐ أدخل مفتاحك المولَّد في البرنامج (نسخة **غير معدّلة**!)
☐ المتوقع: «تم تفعيل ميزان Pro بنجاح!» + بطاقة خضراء في الإعدادات

---

## التجربة 5 — متقدمة (اختيارية): فك ملف الحالة (15 دقيقة)

ملف `%AppData%\MizanPro\state.enc` هو المسار الاحتياطي — محتواه Base64 ثم
AES-128-CBC.

☐ ابحث في dnSpy عن حقلَي المفتاح وIV داخل `ProductStateEngine`
   *(تلميح: مصفوفتان من bytes تُبنى من نص ثابت — Use Search for Constants/Literals)*
☐ استخدم Python + `pycryptodome` لفك الملف واقرأ محتواه

```python
from Crypto.Cipher import AES
import base64

KEY = b"..."   # 16 بايت — استخرجهما من التجميعة
IV  = b"..."   # 16 بايت

raw = open(r"%AppData%\MizanPro\state.enc".replace("%AppData%", __import__("os").environ["APPDATA"]), "r").read()
plain = AES.new(KEY, AES.MODE_CBC, IV).decrypt(base64.b64decode(raw))
print(plain)   # ماذا تعني المحتويات؟ وما علاقتها بمعرّف الجهاز من التجربة 0؟
```

**سؤال:** لماذا يُعدّ تضمين مفتاح التشفير داخل التجميعة نقطة ضعف جوهرية؟

---

## نقاش الدفاع الختامي (10 دقائق)

اقترحوا ثلاث آليات تجعل كسر هذه الحماية أصعب عملياً. نقاط للتفكير:
- مفاتيح مربوطة بالجهاز + توقيع رقمي (RSA) بدل خوارزمية حسابية قابلة للاشتقاق
- فحص تكامل ذاتي للتجميعة (Self-Integrity Check) ومزج النتائج بمنطق البرنامج
- تنفيذ التحقق على خادم بعيد، وتفريق/تعمية (Obfuscation) مسارات الحماية

---

## التسليمات

| التسليم | الشكل |
|---|---|
| تقرير التجارب (أسئلة + جدول الاستخراج من التجربة 2) | PDF |
| MizanPro-Patched.exe أو لقطة شاشة للتعديل في dnSpy | ملف/صورة |
| keygen.py يعمل على نسخة غير معدّلة | ملف + لقطة نجاح التفعيل |
| (اختياري) سكربت فك state.enc | ملف |
