# تقرير التدقيق — ميزان Pro + ورشة الكسر

**التاريخ:** 2026-09-13 · **الغرض:** مراجعة كاملة قبل عرض غدٍ أمام الطلاب
**ما يغطّيه هذا الملف:** (أ) فحص الصور السبعة في `the problem/` (ب) أسباب الأخطاء في الكود مع أمر الإصلاح الدقيق (ج) 4 مشاكل **توقف الورشة** إن لم تُحل (د) قائمة مهام جاهزة للـ AI Agent (هـ) أخطاء في `CrackGuide.md` نفسه.

> لا يوجد في هذا الملف أي تعديل على الكود — هو **مواصفات إصلاح** تُسلَّم للـ Agent.
> كل رقم سطر/اسم ملف مذكور هنا تم التحقق منه بالقراءة من نسخة المستودع `3be97a8`.

---

## 0) الخلاصة في 6 أسطر

1. **الصور ليست «شكلها حلو/ وحش» — هي تقرير عن 4 أخطاء حقيقية في التطبيق**، وثلاثة منها تُسقط العرض أمام الطلاب (نافذة خطأ `Object reference…` + جدول فواتير فاضي + صفحة منتجات فاضية).
2. السبب الجذري واحد لثلاثة من الأخطاء: **معاملات أحداث تُطلَق أثناء `InitializeComponent`** قبل أن تكون بقية عناصر XAML قد وُلدت ← `NullReferenceException`.
3. هناك **رابع بنفس Cause**: فتح نافذة «فاتورة جديدة» يُنشئ انهياراً — وهذه أخطر لأنك ستضغطها على المسرح.
4. **أكبر خطر على الورشة ليس في الكود بل في إعدادات النشر:** `PublishSingleFile=true` يجعل `dnSpy` يعجز عن فتح `MizanPro.exe` أصلاً ← المراحل 2 و4 من `CrackGuide.md` **لا يمكن تنفيذها كما هي**.
5. `x32dbg` وحده لا «يكسر» تطبيق .NET (IL + JIT) — لكن يمكن جعله **نجيب العرض** إذا استخدمته للمستوى الذي يبرع فيه (تعديل ذاكرة/سجلات/نقاط توقف) كما هو مفصّل في `Crack_Guide_x32dbg.pdf`.
6. كل الأرقام في خطة الحماية **سليمة رياضياً** وتحققت منها بنفسي: المفتاحان المعلنان صحيحان، وبرهان استحالة `0x2A` صحيح، ومولّد المفاتيح يعمل (نتائج جاهزة في §5).

---

# (أ) فحص الصور — صورة بصورة

المجلد: `the problem/` — 7 لقطات، كلها للتطبيق نفسه (WPF) وهو يعمل فعلياً، أي: **كل شذوذ تراه هو خطأ حقيقي** وليس «مك-أب».

### صورة 1 — `214915.png` (نافذة تسجيل الدخول) ✅ سليمة + 3 ملاحظات
| ما يظهر | الحكم |
|---|---|
| «٦ أيام متبقية من فترة التجربة» + شريط ذهبي مقطوع أسفله | ✔ منطقي (7 − 1 يوم). ⚠ لكن **الشريط تحت النص مقطوع من الحافة** — عنصر `ProgressBar`/زخرفة بلا `Margin` سفلي |
| كلمة المرور تحتوي **نقطة واحدة** قبل الكتابة | ⚠ `PasswordBox` يعرض محرف الإخفاء؛ لا مشكلة — لكن لاحظ أن `RevealButton.Content` يبدأ `●` بينما المنطق `reveal = PasswordBox.Visibility == Visible` يعني أن أول ضغطة **تُظهر** لا تُخفي ← المعاينة الأولى لزر «الإظهار» مقلوبة بصرياً |
| الحقول فارغة + `مثال: admin` كـ placeholder | ✔ |
| **غياب كامل لسطر «تفعيل المنتج»** في هذه اللقطة | ⚠ زر «تفعيل المنتج» موجود داخل `TrialPanel` (أسفل «أو») لكنه **تحت الحافة السفلية** — النافذة `1227×757` والمحتوى أطول: الزر غير ظاهر في اللقطة. أي أنك ستبحث عنه على المسرح |
| لا يوجد أي إطار نافذة/زر إغلاق ظاهر في أعلى اليمين | ✔ نافذة `WindowStyle=None` — لكن لاحظ ⚠ **`×` في أعلى اليسار فقط**؛ في عرض البروجكتور سيشكو أحد الطلاب من «كيف أخرج؟» |

### صورة 2 — `215210.png` (لوحة التحكم) ⚠ 5 ملاحظات
| ما يظهر | التشخيص |
|---|---|
| «إجمالي الإيرادات ٢٬٣٦٢٫١٠ ر.س» = **مبلغ فاتورة واحدة بالتمام** | ✔ الحساب صحيح (الشهر الحالي فيه فاتورة واحدة فقط — `GetMonthlySummaryAsync(now.Year, now.Month)`) ✘ لكن **العنوان يكذب**: «إجمالي الإيرادات» يوحي بكل الفترة. أعد التسمية: «إيرادات الشهر الحالي» |
| «المصروفات ٠ ر.س» دائماً | ✘ **خطأ بيانات**: `DataSeeder` يولّد 12 فاتورة **جميعها `InvoiceType.Sales`** → لا مشتريات إطلاقاً → المؤشر ميت في كل عرض |
| خلط الأرقام: `INV-2026-00012` لاتينية مقابل `٢٬٣٢٫١٠` عربية | ⚠ تناسق: نصوص قاعدة البيانات لاتينية و`ToString("N2")` يستخدم ثقافة `ar-SA`. قرّر واحدة (الأنسب للعرض: لاتينية `InvariantCulture`) وإلا ستبدو الجداول «مكسورة» للتصوير |
| شارات الحالة: «مسددة» في الجدول مقابل «مدفوعة» في مكان آخر | ✘ **تعارض مصطلح + خطأ إملائي**: `Converters/InvoiceStatusToTextConverter.cs` → `"مسددة"` بينما `Pages/InvoicesPage.xaml.cs::InvoiceStatusText` → `"مدفوعة"`. والصواب إملائياً **«مُسَدَّدة»** أو الأفضل **«مدفوعة»** موحّداً |
| الأفاتار: «ال» لـ شركة الوطنية، و«ال» لمؤسسة النور، و«ال» لمجموعة الخليج… | ⚠ `Controls/AvatarHelper.cs::Initials` يأخذ أول حرف من أول كلمتين ← وكل أسماء العملاء تبدأ بـ«الـ» → **كل الأفاتار متطابقة**. وثمّة خطأ في توثيق الدالة نفسها: `مثال: "شركة الوطنية للتجارة" → "وال"` والصواب `"أل"`. الإصلاح المقترح: أول حرفين من **الكلمة المميزة** (بعد حذف شركة/مؤسسة/…): «الوط»، «النو»، «الخل» |
| أعلى اليسار: أيقونة المستخدم + جرس عليه `٣` | ✔ صحيح: 4 فواتير `Issued`، منها 3 متأخرة (وهو ما يعرضه `LoadBellBadgeAsync`) |
| «تنشيط Windows — انتقل إلى الإعدادات» في خلفية كل اللقطات | ✘ **مظهر غير احترافي أمام الدكتور**: فعّل ويندوز العرض أو استخدم `slmgr /rearm` أو صوّر داخل VM نظيفة، أو صغّر النافذة حتى لا تظهر العلامة |

### صورة 3 — `215233.png` (الفواتير + نافذة خطأ) ❌ **أهم صورة**
ما يحدث فعلياً: المستخدم ينقر «العملاء» ← انهيار داخل مُنشِئ الصفحة ← `App.DispatcherUnhandledException` يعرض «حدث خطأ غير متوقع: Object reference not set to an instance of an object» ← الصفحة المعروضة تبقى **الفواتير** بينما التمييز الجانبي على **«العملاء»**.

| العَرَض | السبب الجذري (تم إثباته من الكود) |
|---|---|
| `NullReferenceException` عند فتح «العملاء» | `Pages/CustomersPage.xaml:23` — `ToggleButton x:Name="CardsToggle" IsChecked="True" Checked="CardsToggle_Checked"`. الحدث يُطلَق **أثناء** `InitializeComponent`، والمُعالِج يلمس `CardsList` / `ListHost` / `EmptyCardsText` وهي معرّفة **بعده** في الشجرة ← `null` |
| جدول الفواتير **بلا أي صف** + «عرض 0-0 من 0» | `Pages/InvoicesPage.xaml:33-34` — `StatusCombo` فيه `IsSelected="True"` + `SelectionChanged="Filter_Changed"` ← `LoadAsync()` يُستدعى أثناء البناء ← `Loader.Visibility` على `null` ← استثناء داخل `try` ثم **`finally` ينفجر أيضاً** على نفس السطر، والنتيجة أن `_loading` **يبقى `true` للأبد** ← كل `Refresh()` لاحق يُتجاهل بصمت (`if (_loading) return;`) |
| الصفحة لا تُصلَح أبداً بعد الخطأ | نفس السبب: علم `_loading` عالق + `catch` يعرض رسالة فقط ولا يعيد المحاولة |
| التاريخ في العمود «التاريخ» يعرض `٢٦/٠٩/٢٠٢٦` وهو **مستقبلي** | `DataSeeder` يوزّع الفواتير بـ `AddDays(-DaysAgo)` حول `DateTime.Today`؛ عند لقطة اليوم 13/09 ظهر 26/09؟ ← راجع: إن كانت اللقطة قديمة (26/08 مثلاً) فهذا **تاريخ منتهي الصلاحية للبيانات**. العبرة: البذور «حية» نسبية لليوم، لذا **لقطات العرض يجب أن تُعاد بعد الإصلاح مباشرة**، لا قبلها بيوم |
| حقلا التاريخ يعرضان `Select a date` بالإنجليزية | ✘ ثقافة الواجهة غير مضبوطة: لا يوجد `FrameworkElement.LanguageProperty`/`xml:lang`، فالـ `DatePicker` يستخدم `en-US` ← **لمسة سهلة تعطي انطباعاً احترافياً**: اضبط `Thread.CurrentThread.CurrentUICulture = new CultureInfo("ar-SA")` (أو `Language="ar-SA"`) + صياغة `yyyy/MM/dd` |
| الحقل الأول في شريط الفلاتر يظهر فارغاً بلا `placeholder` | ⚠ `ModernTextBoxStyle` يعرض `Tag` كنص إرشادي — يعمل؛ لكن في لقطة المنتجات (صورة 4) `CategoryCombo` فارغ تماماً لأن التحميل لم يحدث أصلاً (نفس العطل) |

### صورة 4 — `215301.png` (المنتجات) ❌
| العَرَض | السبب |
|---|---|
| **لا توجد بطاقات منتجات** و«كل الفئات» غير موجود في القائمة | `Pages/ProductsPage.xaml:36-37` — نفس فخ `IsSelected="True"` + `SelectionChanged` → `LoadAsync` ينفجر أثناء التهيئة، ثم `_loading` عالق |
| `ComboBox` **فاضل تماماً** بلا عنصر محدد بجانب «كل حالات المخزون» | `CategoryCombo` لا يُملأ إلا داخل `LoadAsync` (الميتة) — صمّمها أسوأ: بناء القائمة يتم داخل دورة التحميل نفسها، فأيّ فشل يُبقي الفلتر أعرج. انقل بناء قائمة الفئات إلى `OnInitialized`/المنشئ |
| العنوان «المنتجات» و**سطر الوصف خلف زر «منتج جديد +» مباشرة** والنص مقطوع | ✘ **خطأ تخطيط RTL منهجي** (انظر §2): `HorizontalAlignment="Left"` داخل حاوية `FlowDirection="RightToLeft"` **يُقلب** فيظهر العنصر على اليمين فوق العنوان. الخطأ في كل صفحات `Pages/*.xaml` التي تستخدم هذا النمط (فواتير/عملاء/منتجات/تقارير) |

### صورة 5 — `215323.png` (التقارير) ❌ 3 أعطال
| العَرَض | السبب |
|---|---|
| قائمة «توزيع الفواتير حسب الحالة» غير مقروءة: النقاط الملوّنة فوق النص، والـ 4 أسطر تبدو متطابقة | ✘ نفس سبب RTL: في `Pages/ReportsPage.xaml` الـ `Grid` لكل سطر يحتوي `StackPanel` (نقطة+تسمية) بلا محاذاة، والنص الثاني `HorizontalAlignment="Left"` ← يُقلب إلى اليمين فيتراكب فوق الأول. **الحل الصحيح**: عمودان `ColumnDefinition Width="*"` / `"Auto"` بدل الاعتماد على المحاذاة |
| «عدد الفواتير ١» بينما الجدول الرئيسي يعرض 12 فاتورة | ⚠ **ليس خطأ حسابياً** (التقرير شهري والشهر فيه فاتورة واحدة) — لكنه خطأ **تواصل**: ضع عنوان الفترة داخل البطاقة («سبتمبر 2026») وإلا سيفهم الحضور أن هناك خطأ في التقارير |
| «ضريبة القيمة المضافة ١٥٪ ٣٠٨٫١٠» و«قبل الضريبة ٢٬٠٥٤٫٠٠» و«الإجمالي ٢٬٣٦٢٫١٠» | ✔ الأرقام **متطابقة تماماً** مع الاستحقاق (2054 × 0.15 = 308.10؛ 2054+308.10 = 2362.10) — استخدمها في العرض كـ«دليل دقة المحرك» |
| صناديق الشهر/السنة **تتراكب على سطر العنوان** أعلى يمين المحتوى | ✘ نفس عيب الـ Grid بلا أعمدة |
| لا يوجد رسم بياني أصلاً رغم عنوان «توزيع … حسب الحالة» | ⚠ عرض نصي فقط (نقاط + أعداء) — مقبول، لكن سمِّه «ملخص الحالة» أو أضف أشرطة بسيطة، وإلا سيسألك الدكتور «وين الرسم؟» |
| «المسدد» / «المتبقي» | ⚠ إملاء: **«المُسَدَّد»**؛ و«المتبقي» يشمل `Draft` أيضاً (`Status != Paid`) أي أنه ليس «متبقي التحصيل» الحقيقي — اما أن تستثري المسودات أو تسمّيها «غير المسددة» |

### صورة 6 — `215341.png` (الإعدادات ← الشركة) ❌
| العَرَض | السبب |
|---|---|
| الحقول تعرض **نصوص إرشادية** (`مثال: مؤسسة ميزان…`، `info@company.sa`) بدل بيانات محفوظة | ✔ سلوك صحيح لبرنامج جديد (لا شيء محفوظ) — ⚠ لكنه **يظهر كأنه باگ** أمام جمهور. الحل: املأ البيانات في `Data/` أو اضبط قيماً افتراضية في `AppSettings` عند أول تشغيل، أو صوّر بعد الضغط على «حفظ» |
| زر «حفظ بيانات الشركة» **مقطوع من الأسفل** ومطلّي على علامة تنشيط ويندوز | ✘ تخطيط: الحاوية `StackPanel` داخل `ScrollViewer`؟ النافذة أطول من الارتفاع، والزر خارج View — اضبط `ScrollViewer.VerticalScrollBarVisibility="Auto"` على مستوى التبويب أو قلّل الحشو |
| «الرقم الضريبي» يعرض `٣XXXXXXXXXXXXXXX` و«الهاتف» `٠٥XXXXXXXX` | ⚠ نص إرشادي يمزج أرقاماً عربية مع `X` لاتينية = فوضى بصرية؛ اجعل `Mask`/النص موحداً (`300000000000000` مثال لاتيني) |
| تبويبات: الشركة / الحساب / الترخيص / التفضيلات / عن البرنامج بالترتيب الصحيح | ✔ |

### صورة 7 — `215449.png` (سطح المكتب + نافذة التفعيل) ❌ **مصادر إحراج على المسرح**
| ما يظهر | الحكم |
|---|---|
| نافذة «تفعيل ميزان Pro» مفتوحة وفي حقل المفتاح **`26F30-20C2B-22E4B-B`** | ✘⚠ **هذه بصمة الجهاز، وليست مفتاحاً!** تم لصقها في حقل المفتاح فقام المُشكِّل التلقائي (`KeyBox_TextChanged`) بإعادة تقسيمها إلى مجموعات من 5: `26F3 020C 2B22 E4BB` (16 محرفاً بصيغة 4-4-4-4 من `TokenProcessor.ComputeMachineFingerprint`) → `26F30-20C2B-22E4B-B` (5-5-5-1). الرسالة «مفتاح الترخيص غير صالح» صحيحة ✔ لكن **الحقل يقبل لصق أي شيء ولا يميّز**: أضف تلميحاً فورياً «المفتاح 20 محرفاً — الحالي 16» بدل رسالة عامة، وإلا سيضيع الطلاب غداً |
| «معرّف الجهاز» خلف النافذة يظهر مقطوعاً مع زر «نسخ» | ⚠ تراكب/حجم خط؛ ولأن `Clipboard.SetText` داخل `try/catch` صامت فإن فشل الحافظة (شائع جداً) **لن يُخبر أحداً** — اجعل رسالة الفشل مرئية، وضع `TextBox` قابل للتحديد بدل الاعتماد على النسخ |
| لافتة الترخيص: «٦ من ٧ أيام متبقية» بينما القائمة الجانبية «تجربة · ٦ أيام» | ✔ متسق |
| سطح المكتب مليء بأيقونات (Claude Setup.exe، Antigravity، Chatbox، «mware.exe اختصار»، «StopDownl…»، ملف `script`) + Taskbar مزدحم | ✘ **نظّف سطح المكتب قبل العرض** (مجلد واحد + اختصار MizanPro). «mware.exe» و«StopDownl…» وحدهما كفيلان بسؤال محرج من الدكتور 🙂 |
| نوافذ طرفية سوداء خلف التطبيق، وعلامة «تنشيط ويندوز» | ⚠ أغلقها؛ واحذف `Windows 11/10 unactivated` (انظر صورة 2) |
| حجم اللقطة 1920×1058 بينما الأخريات ~1600×970 | ⚠ **مقياس مختلف بين اللقطات** = شرائح غير متساوية في البوربوينت. أعد التصوير كلها على نفس الدقة/التكبير (1920×1080، تكبير 100%) |

> **ملاحظة على الصور نفسها:** الأسماء `تعليق توضيحي …` لا تحمل أي شرح، ولا يوجد تحديد/أسهم على مواضع الخطأ. لإصلاح سريع: أعد التصوير باسم `01-login.png`…`07-desktop.png` وضع سهماً أحمر على موضع المشكلة — وإلا ستقضي وقتاً في البرزنتيشن في «شوفوا وين الخطأ؟».

---

# (ب) الأعطال الجذرية في الكود — مواصفات الإصلاح

مرتّبة بالخطورة. كلها **أعطال واجهة** لا تلتقطها الـ CI (انظر §4).

### 1. 🔴 `Object reference…` + شلل تام للصفحة: أحداث تُطلَق أثناء تهيئة XAML
**الملفات:**
- `Pages/CustomersPage.xaml:23-27` (`IsChecked="True"` + `Checked=`)
- `Pages/InvoicesPage.xaml:33-34` (`IsSelected="True"` + `SelectionChanged=`)
- `Pages/ProductsPage.xaml:36-37` (نفس النمط)
- `Windows/NewInvoiceDialog.xaml:125-127` (`Text="0"` + `TextChanged="DiscountInput_TextChanged"`) ← `UpdateSummary()` يلمس `TotalText` المعرّف في السطر 134 ← **فتح «فاتورة جديدة» ينهي العرض**

**القاعدة:** أي معالج حدث في XAML على عنصر يُعطي قيمة ابتدائية (`IsChecked`/`IsSelected`/`Text`/`IsExpanded`) **قد يعمل قبل أن تُنشأ العناصر اللاحقة**.

**مواصفات الإصلاح (اختر 1 + 2 معاً، وهما رخيصان):**
1. **حرّاس تهيئة:** أول سطر في كل معالج حدث «خطير»: `if (!IsInitialized) return;` — يشمل `CardsToggle_Checked`, `ListToggle_Checked`, `Filter_Changed`, `SearchInput_TextChanged`, `Period_Changed`, `DiscountInput_TextChanged`.
2. **لا قيم ابتدائية في XAML للمُطلِقات للأحداث:** احذف `IsChecked="True"` / `IsSelected="True"` / `Text="0"` واضبطها من الكود **بعد** `InitializeComponent()` (أو داخل `OnInitialized`).
3. **حصّن `LoadAsync` في كل الصفحات:**
   - انقل `_loading = true` إلى **داخل** `try`، واجعل الـ `finally` لا يلمس أي عنصر واجهة بلا فحص (`if (Loader != null)` أو `Dispatcher.BeginInvoke`)؛
   - بعد `catch` **أعِد المحاولة تلقائياً مرة واحدة** عبر `Loaded += (_,_) => Refresh();` بدل الاكتفاء برسالة `MessageBox`؛
   - استبدل «إسكات الطلب أثناء التحميل» بجدولة: بدل `if (_loading) return;` → `_pending = true;` ثم عند الانتهاء `if (_pending) Refresh();` (حتى لا تفقد التحديثات).
4. **اختبار قبول:** بعد الإصلاح، افتح «العملاء» ثم «الفواتير» ثم «المنتجات» ثم «فاتورة جديدة» — لا يجب أن تظهر أي نافذة خطأ، ويجب أن تمتلئ الجداول من أول مرة.

### 2. 🔴 تراكب النصوص في RTL: `HorizontalAlignment="Left"` ينقلب يميناً
**الملفات:** `Pages/InvoicesPage.xaml:19`، `Pages/CustomersPage.xaml:20`، `Pages/ProductsPage.xaml:13`، `Pages/ReportsPage.xaml:18`، `Pages/ReportsPage.xaml` (كل سطر في `StatusList`)، وأي `Grid` بلا أعمدة يضع عنوانين في نفس الخلية.
**الحقيقة:** في WPF، مع `FlowDirection="RightToLeft"` تُعامَل `Left`/`Right` كـ Start/End **معكوسة**: `Left` = الجانب الأبعد = **اليمين فعلياً**. لذلك «زر الإجراء» و«صناديق الشهر» و«قيمة السطر» كلها قفزت فوق العناوين.
**مواصفات الإصلاح:**
- لكل «هيدر صفحة»: استخدم `Grid` بعمودين — `ColumnDefinition Width="*"` (العنوان) و`Width="Auto"` (الأزرار) — ولا تعتمد على المحاذاة المتراكبة.
- داخل `StatusList`: عمودان `*` و`Auto` + `HorizontalAlignment="Right"` على الرقم (لأن `Left` تعني هنا اليمين — أو الأسلم: اتركها `Right`/`Left` **بعد** الاختبار البصري، أو استخدم `FlowDirection="LeftToRight"` على الـ Grid الداخلي للسطر).
- اختبار قبول: لقطة لكل صفحة؛ لا يجب أن يلمس أي زر نص العنوان.

### 3. 🟠 ثقافة الواجهة غير مضبوطة (تواريخ/أرقام إنجليزية + `Select a date`)
**المواصفات:** في `App.xaml.cs::OnStartup` (قبل أي نافذة):
`Thread.CurrentThread.CurrentCulture = new CultureInfo("ar-SA");` + `CurrentUICulture` نفس الشيء، مع `culture.DateTimeFormat.Calendar = new GregorianCalendar();` — وأنشئ `FrameworkElement.LanguageProperty.OverrideMetadata(typeof(FrameworkElement), new FrameworkPropertyMetadata(XmlLanguage.GetLanguage("ar-SA")))` حتى يتغيّر نص `DatePicker` المائي ويتسق تنسيق التاريخ.
**تنبيه قرار:** عند ضبط `ar-SA` ستتحول **كل** الأرقام إلى `٦ ٧ ٢٬٠٥٤٫٠٠` (وهو ما تراه في اللقطات) — إن أردت أرقاماً لاتينية أنيقة أسهل للقراءة على البروجكتور، استخدم `"ar"` مع `NumberFormat.NumberDigitSubstitution = None` أو اضبط `CultureInfo("ar-SA"){NumberFormat={NativeDigit=...}}`/أو ببساطة استخدم `InvariantCulture` في كل `ToString` (الأوضح للتقارير + يطابق `INV-2026-00012`). **قرّر واحدة وطبّقها في كل مكان** — الخلط الحالي هو المشكلة الحقيقية.
كما يجب توحيد كل `ToString("yyyy/MM/dd")` (موجود في `NewInvoiceDialog`, `InvoicesPage::ViewInvoice_Click`) مقابل `dd/MM/yyyy` في الجداول.

### 4. 🟠 `App.DispatcherUnhandledException` يبتلع الأعطال
`App.xaml.cs:24-33` يعرض رسالة ويضع `args.Handled = true` ← التطبيق يواصل نصف ميت (وهذا بالضبط ما جعل صورة 3 و4 تبدوان «غريبين» بدل أن ينهار البرنامج).
**مواصفات الإصلاح (لنسخة العرض):** احتفظ بالـ MessageBox لكن:
- أضف نصاً «انسخ هذا الخطأ للمطوّر» + **سجّل الاستثناء كاملاً** في `%AppData%\MizanPro\error.log` (`File.AppendAllText` مع `ex.ToString()`)؛
- وأضف خيار «إعادة تشغيل» بدل الصمت؛
- وإن أردت ضبطاً أكاديمياً: في بناء `Workshop` اجعله `Environment.Exit(1)` حتى تُرى المشكلة (أنت لن تريد حالة نصف-مكسورة أثناء شرح الكسر).

### 5. 🟠 `DataSeeder`: لا مشتريات، والمخزون لا يتغيّر، والتواريخ مطلقية
`Data/DataSeeder.cs` — كل الفواتير `InvoiceType.Sales` (يُبنى بها `BuildInvoice` دون تمرير النوع) ⇒
- مؤشر «المصروفات» = 0 دائماً في لوحة التحكم (يبدو كتقصير في مشروع «نظام محاسبة»!);
- **مخزون البذور غير مطبّق**: `BuildInvoice` لا ينقص `product.Stock` رغم أن الفواتير «صادرة/مسددة» ⇒ أرقام المخزون المعروضة لا تحكي قصة، و«منخفض المخزون» لا يتغيّر بعد إصدار فاتورة من البذور؛
- الحل: أضف 3 مشتريات + مرتجع واحد، ومرّر `Type` إلى `BuildInvoice`، وطبّق حركة مخزون متسقة مع القاعدة (`Sales` ينقص، `Purchase/Return` تزيد) للمسودات/الصادرة/المدفوعة، واحذف الفواتير ذات التواريخ المستقبلية (`IssueDate` ≤ اليوم) — وإلا ظهرك «٢٦/٠٩/٢٠٢٦» في «آخر الفواتير».
**اختبار قبول:** «المصروفات» > 0، و`Stock` كل منتج = بذرة − مبيعات + مشتريات (افحص يدوياً منتجاً واحداً على السبورة — سؤال دكتور نموذجي).

### 6. 🟡 `Customer.Balance` لا يُحدَّث بعد السداد/الإلغاء
يُحسب مرة واحدة في `DataSeeder` (أسطر «أرصدة العملاء») ولا يلمسه `InvoiceService.CreateAsync` / `UpdateStatusAsync` ⇒ «شريط الديون» ولوحة العميل **كاذبان بعد أي عملية**.
**مواصفات الإصلاح:** استخرج `RecalculateCustomerBalanceAsync(int customerId)` تُنادى بعد كل `SaveChangesAsync` في الإنشاء/تحديث الحالة/الإلغاء؛ أو (الأفضل معمارياً والأقل كوداً) احذف الخاصية واعرض `Balance` محسوباً في `CustomerService.GetStatsAsync` واستخدم `stats.BalanceDue` في الواجهة بدل `customer.Balance`.
**اختبار قبول:** علّم فاتورة «مدفوعة» من الفواتير ← ديْن العميل في صفحة العملاء يجب أن ينخفض أمامك (لقطة شاشة = مادة دسمة للعرض: «الحسابات حية»).

### 7. 🟡 `ReportsPage`: «إلى تاريخ» يشمل أول يوم من الشهر التالي
`Pages/ReportsPage.xaml.cs`: `var end = start.AddMonths(1);` ثم `GetAllAsync(from: start, to: end)` بينما `InvoiceService.GetAllAsync` يفسّر `to` على أنه **يوم كامل** (`i.IssueDate < to.Date.AddDays(1)`) ⇒ تقرير سبتمبر يشمل فواتير 1 أكتوبر. (الملخص الشهري نفسه صحيح لأنه يستخدم `end` مباشرة.)
**الإصلاح:** مرّر `to: end.AddDays(-1)` (أو `end - 1 tick`)؛ وأضف فحصاً في `.github/smoke` لهذه الحالة الحدودية.

### 8. 🟡 منطق `RevealButton` مقلوب + حالة النص
`Windows/AuthWindow.xaml.cs::RevealPassword_Click` يبدأ و`PasswordBox` ظاهر ⇒ `reveal == true` ⇒ أول ضغطة تعرض النص. الرمز `●/○` مُثبّت عكسياً. اجعل الحقل حالةً واحدة (`_revealed`) وحدّث `Content`/`ToolTip` منه، ولا تعتمد على `Visibility` كمصدر حقيقة.

### 9. 🟡 `SidebarHost.Width = to` بعد `BeginAnimation` — سطر ميت
`Windows/MainWindow.xaml.cs::ToggleSidebar_Click`: القيمة المحلية لا تطبَّق أثناء/بعد أن تكون خاصية `Width` مُحرَّكة (`HoldEnd`). يعمل بالصدفة. الإصلاح: `animation.Completed += (_,_) => { SidebarHost.BeginAnimation(WidthProperty, null); SidebarHost.Width = to; }`.
وفي `SetSidebarLabels`: النص يختفي فوراً عند التقليص ويظهر بعد 210ms — اجعل الظهور مرتبطاً بـ`Completed` بدل `DispatcherTimer` بقدر ثابت.

### 10. 🟢 تكرار لوحة التفعيل (صيانة مستقبلية)
`Windows/AuthWindow.xaml` يعيد تنفيذ لوحة التفعيل يدوياً (`KeyBox_TextChanged`, `ActivateAsync`, التنسيق) بينما `Controls/ActivationPanel.xaml(.cs)` هو المكوّن الأصلي (المستخدم في نافذة الإعدادات فقط). نسختان من نفس منطق التنسيق = سطران لكل إصلاح. **المواصفة:** استخدم `Controls:ActivationPanel` داخل `AuthWindow` واحذف النسخة المكررة، ووثّق ذلك في `README.md` (سطر «لوحة إدخال مفتاح التفعيل (…)» يوحي بأنها مستخدمة في الدخول — غير صحيح الآن).

---

# (ج) 4 مشاكل **تُوقف** الورشة (أهم من كل ما سبق)

### ⛔ 1. `PublishSingleFile=true` يقتل المراحل 2 و4 و5 من `CrackGuide.md`
- `MizanPro.csproj` + `build.bat` + workflow: `PublishSingleFile=true` + `IncludeNativeLibrariesForSelfExtract=true`.
- النتيجة: **لا يوجد `MizanPro.dll` على القرص إطلاقاً** — التجميعة المُدارة داخل حزمة مضمّمة في الـ apphost، لا تُستخلص (تُستخلص المكتبات الأصلية فقط).
- لذلك: dnSpy (6.41/dnSpyEx) **لا يستطيع فتح `MizanPro.exe` لرؤية IL**، و«`Edit Method` → `Save Module` → `MizanPro_Cracked.exe`» **مستحيل تقنياً** (لا يمكن إعادة كتابة ملف تنفيذي apphost مع حزمته عبر dnSpy).
- **الإصلاح المطلوب (قرار واحد يفتح كل شيء):** انشر **مكتفٍ ذاتياً بدون ملف واحد**:
  ```
  dotnet publish -c Release -r win-x86 --self-contained true -p:PublishSingleFile=false -o dist
  ```
  ⇒ في `dist\` يوجد `MizanPro.dll` (وهو الهدف الفعلي للتحليل) + `MizanPro.exe` (apphost 32-بت لتشغيله) + كل مكتبات `System.*`.
  ⇒ **فائدة مضاعفة حاسمة:** لأن الحزم أصبحت بجانب `MizanPro.dll`، يستطيع dnSpy أن **يحلّل مراجعها** عند `Edit Method (C#)` — وهذا هو الشرط العملي لكي يعمل محرر C# على تجميعة net6.0 (التي عادةً تُفشل dnSpy بـ «Could not load System.Runtime»).
  ⇒ لا تزل `SelfContained` — لأن حمله على أجهزة الطلاب يعني طلب «.NET 6 Desktop Runtime» على 20 جهازاً غداً؛ ابقِ المجلد كاملاً.
- **تبعات إلزامية:** `Workshop_Pack.bat` currently copies **a single file** → يجب أن ينسخ محتويات `dist\` كاملة (مجلد!)، و`README_Workshop.md`/`Tools_Links.txt`/`LabSheet.md` يجب أن تقول «شغّل `MizanPro\MizanPro.exe`» لا «انسخ ملفاً واحداً». وأضف للـ `.gitignore` ملاحظة أن `dist/` مجلد وليس ملفاً.
- **بديل طوارئ** (لو لم ينجح البناء في الوقت): احتفظ بالنسخة أحادية الملف وألغِ المراحل 2 و4؛ واشرح المراحل 1 و3 و5 فقط. **لا تصعد وأنت تنوي فتح exe أحادي الملف في dnSpy أمام الطلاب** — ستفشل على المسرح.

### ⛔ 2. `x32dbg` وحده لا يكفي لتطبيق .NET — والخطة الحالية «مكسرة» في تفصيلها
- `CrackGuide.md` المرحلة 3 يأمر بـ `bpdll user32.dll` ← **هذه ليست أمراً موجوداً في x64dbg** (الأمر الصحيح: `bp user32.dll:MessageBoxW`، أو `SetBreakpointOnDllLoad`، أو ضبط Options→Events→«Break on DLL load»). انشر التصحيح أو سيضيّع الفريق 4 دقائق في «unknown command».
- نفس المرحلة تتحدث عن إطارات في **`clr.dll`** — لكن .NET 6 (CoreCLR) يحمّل **`coreclr.dll`** (و`clrjit.dll`). صحّح الاسم أو سيعلق أحد الطلاب بسؤال دقيق أمام الدكاترة.
- «عدّل EAX إلى 0» ثم «التطبيق يغلق كالمعتاد»: **هذا ليس كسراً ولا يغيّر أي سلوك** — لأن `MessageBox.Show` هنا نتيجته غير مستخدمة (`App.xaml.cs`). إن قُدِّم للطلاب كأنه «كسر» فسينتقدك المشرف منهجياً. في `Crack_Guide_x32dbg.pdf` أعدت بناء هذه المرحلة لتكون **كسراً حقيقياً بنفس الأداة** (تعديل IL في الذاكرة + لماذا التوقيت يقرّر + `TieredCompilation`)، وأبقيت `MessageBoxW/EAX` كتمرين لفهم السجلات (لا ككسر).
- **الخلاصة المنهجية التي يجب أن تقولها بصوت عالٍ غداً:** «.NET يُكسر على مستوى IL؛ والـ debugger الأصلي يُظهر أوامر المعالج بعد الترجمة اللحظية — ولهذا سنُري الطابقين، ونشرح لماذا اختيار الأداة هو نصف الحل». هذا يحوّل «إشكالية» إلى **نقطة قوة أكاديمية** (وهو بالضبط ما تبحث عنه مادة الهندسة العكسية).

###  3. البذور والصور غير متزامنة مع الكود
لقطات `the problem/` لا تطابق ما سيُعرَض غداً (بيانات تاريخية + أعطال ستُصلَح). كل شريحة فيها صورة قديمة = سؤال محرج. **المطلوب:** بعد تنفيذ الإصلاحات (1) و(2) من §ب، أعد بناء `dist\`، نظّف حالة الترخيص، **وأعد تصوير 6 لقطات بأسماء نظيفة ودقة موحّدة 1920×1080**، ثم استُخدمها في `MizanPro_Crack_Workshop.pptx` (مكان الصور محجوز ومُعلَّم).

###  4. حقل المفتاح يقبل 24 محرفاً بلا تغذية راجعة فورية
`MaxLength=24` + «خطأ عام» بعد الضغط — الطلاب سيعلقون هنا بالعشرات غداً (انظر صورة 7: بصمة جهاز من 16 محرفاً لُصقت كـ«مفتاح»).
**المواصفات:** عدّاد حيّ تحت الحقل (`17/20`) + رسالة مختلفة لكل حالة: «الطول يجب 20 بعد حذف الشرطات (حالي: 16)» / «يوجد محرف غير مسموح: …» / «المفتاح صحيح لكن لا يستوفي الشرط الحسابي». هذا الإصلاح **يدرّس نفسه**: الطالب يميّز فورياً بين خطأ التنسيق وخطأ الخوارزمية — وهي أهم نقطة في المحاضرة.

---

# (د) قائمة مهام جاهزة للـ AI Agent (نفّذ بالترتيب، أوقف عند أول فشل)

| # | المهمة | الملفات | معيار القبول |
|---|---|---|---|
| 1 | إزالة فخاخ أحداث التهيئة (4 مواضع) + حصّن `LoadAsync` (`_loading`, `_pending`, `finally` آمن) | `Pages/{Customers,Invoices,Products,Reports}Page.xaml{.cs}`, `Windows/NewInvoiceDialog.xaml{.cs}` | فتح كل الصفحات و«فاتورة جديدة» بلا أي خطأ، والجداول ممتلئة من أول مرة |
| 2 | إصلاح التخطيط RTL بـ Grid بعمودين للهيدر + أسطر الحالة | `Pages/*.xaml` | لا تراكب نص/زر في لقطات كل الصفحات |
| 3 | توحيد الثقافة والأرقام (قرار واحد) + `DatePicker` عربي | `App.xaml.cs`, كل `ToString` | لا `Select a date`، ولا خلط `٦` مع `6` |
| 4 | سجل أخطاء مرئي + `error.log` | `App.xaml.cs` | أي استثناء يظهر كاملاً في ملف |
| 5 | بذور واقعية: مشتريات/مرتجعات + خصم مخزون البذور + بدون تواريخ مستقبلية | `Data/DataSeeder.cs` (+ حدّث عدّاد `.github/smoke/Program.cs` إذا تغيّر العدد 12) | «المصروفات» > 0؛ فحص يدوي لمخزون منتج واحد يطابق الحركات |
| 6 | `RecalculateCustomerBalance` بعد كل حركة فاتورة | `Core/Services/InvoiceService.cs`, `CustomerService.cs` | تعليم فاتورة «مدفوعة» يخفض دين العميل في صفحة العملاء |
| 7 | حدود تاريخ التقرير الشهري | `Pages/ReportsPage.xaml.cs` | تقرير سبتمبر لا يشمل 1 أكتوبر (فحص CI جديد) |
| 8 | **النشر بلا ملف واحد**: `PublishSingleFile=false` والحزمة مجلد | `MizanPro.csproj`, `build.bat`, `Workshop_Pack.bat`, `.github/workflows/build.yml`, `README.md`, `README_Workshop.md`, `LabSheet.md`, `Tools_Links.txt` | `dist\MizanPro.dll` موجود، وdnSpy يفتحه ويُظهر IL، و`dotnet publish` ينجح، والسير أخضر |
| 9 | ملاحظات فورية في حقل المفتاح (طول/محرف/شرط حسابي) + تمييز «هذه بصمة جهاز» | `Controls/ActivationPanel.xaml{.cs}`, `Windows/AuthWindow.xaml{.cs}`, `Pages/SettingsPage.xaml` | لصق `26F30-20C2B-22E4B-B` يعطي رسالة «طول 16 — المفتاح 20» لا رسالة عامة |
| 10 | تصحيح `CrackGuide.md`: إزالة `bpdll`، `coreclr.dll` بدل `clr.dll`، صياغة «EAX=0» كتمرين لا ككسر، إضافة فصل x32dbg البديل من `Crack_Guide_x32dbg.pdf`، وتحديث مسار المراحل لاسم `MizanPro.dll` | `CrackGuide.md`, `README_Workshop.md`, `Workshop_Slides.md`, `LabSheet.md` | لا يوجد أي أمر في الدليل لا يعمل حرفياً على الجهاز |
| 11 | إعادة تصوير 6 لقطات نظيفة (1920×1080، ويندوز مُنشَّط، سطح مكتب فاضي) باسم `assets/shots/0X-*.png` واستبدالها في الشرائح | `assets/`, `MizanPro_Crack_Workshop.pptx` | كل صورة في العرض مطابقة للإصدار الذي سيُنفَّذ أمام الطلاب |
| 12 | بروفة كاملة مسجّلة (OBS): 40 دقيقة، ونسخة MP4 احتياطية على سطح المكتب | — | فيديو موجود + الدليل يُشغَّل بدون توقّف واحد |

**تحقّق CI إضافي مقترح (رخيص ويحميك غداً):** في `.github/smoke` أضف: (أ) فحص أن `MizanPro.dll` موجود بعد النشر (يمنع رجوع `PublishSingleFile`)؛ (ب) حالة `to=end` في التقرير الشهري؛ (ج) أن `Workshop_Pack.bat` ينتج ≥ 3 ملفات في `Workshop_Delivery\MizanPro\`.

---

# (هـ) حقائق تحققت منها بالأرقام (استخدمها بثقة)

### خوارزمية المفتاح — صحيحة كما موثّقة
`Core/Engine/TokenProcessor.cs`: 20 محرفاً `[A-Z0-9]` بعد حذف `-` و`ToUpperInvariant`، `Σ ASCII % 73 == 0` و `XOR == RequiredXor (0x0A)`.

| المفتاح | المجموع | % 73 | XOR | النتيجة |
|---|---|---|---|---|
| `M1Z4N-2025P-ROM4S-T3R01` | 1314 | 0 | 0x0A | ✅ يقبله التطبيق الأصلي |
| `AAAAABBBBBCCCCCADN89` | 1314 | 0 | 0x0A | ✅ يقبله |
| `AAAAABBBBBCCCCCDE77A` (مثال المسودة الأصلية) | 1302 | 61 | 0x00 | ❌ مرفوض — موثّق صحيح |
| `AAAAA-BBBBB-CCCCC-DDDDD` (المفتاح «التجريبي» في الأدلة) | 1330 | 16 | 0x04 | ❌ مرفوض — صحيح أنه «مثال فشل» |
| `5NOFK-JQB1Z-7HSHF-NOP6D` ← **مفتاح «بعد الرتق» فقط** | 1426 | **39** | **0x0A** | ❌ قبل الرتق / ✅ بعد تغيير `73` إلى `1` في IL |

**متحقَّق أيضاً:** `1426 % 73 = 39` و`1426` ليس مضاعفاً لـ146 (= 146×9.77) — أي أن **شرط المجموع وحده يفشل بينما شرط XOR ينجح**. لهذا يكفي رتق بايت واحد (`73` ← `1`) لقبول هذا المفتاح تحديداً: قصة «الرتق الواحد الذي يُرى أثره فوراً» في الفصل 6 من دليل الـ PDF.

### مفاتيح فاينية (Vanity) جاهزة للعرض — كلها **صحيحة** ومولّدة بالمُشتَقّ
```
MIZAN-2026X-32DBG-B36SQ      (sum 1314 = 146×9, xor 0x0A)
CRACK-MEWOR-KSHOP-BY0A5      (sum 1460, xor 0x0A)
MIZAN-PROCR-ACKED-P4P7L      (sum 1460, xor 0x0A)
GROUP-4SENI-OUR00-JYIP2      (sum 1460, xor 0x0A)
ALBLA-SATEA-M4000-6LFK4      (sum 1314, xor 0x0A)
```
استخدم `MIZAN-2026X-32DBG-B36SQ` في ختام العرض: يولّده الطالب نفسه في 5 ثوانٍ أمام الدكتور.

### برهان استحالة `0x2A` — صحيح، وصياغته الأقوى للعرض
لكل محرف من `[A-Z0-9]`: الأرقام `0x30-0x39` → بت6=0، بت5=1؛ الأحرف `0x41-0x5A` → بت6=1، بت5=0.
⇒ في XOR النهائي: بت6 = زوجية عدد الأحرف، بت5 =زوجية عدد الأرقام؛ ومجموعهما 20 (زوجي) ⇒ **الزوجيتان متساويتان** ⇒ بت6=بت5 دائماً ⇒ القيمتان الوحيدتان Possible للبتين هما `00` أو `11`.
`0x0A` → `00` ✅ ممكن. `0x2A` → `10` ❌ مستحيل. (تحقق عددي من كل الاحتمالات: `{0x00, 0x60}`.)
**مكسب إضافي للعرض:** نفس البرهان يعطي **ثالثاً لم يذكره أحد في الأدلة**: لأن `xor == 0x0A` يجبر `bit0(xor)=0`، فإن **عدد المحارف الفردية زوجي** ⇒ `Σ ASCII` زوجي ⇒ بما أنه يجب أن يقبل القسمة على 73 (فردي) فإن **المجموع يجب أن يكون مضاعفاً لـ 146، لا 73** — أي أن `sum ∈ {1022, 1168, 1314, 1460, 1606, 1752}` فقط. هذان المفتاحان المعلنان كلاهما 1314 ✓. قل هذه الجملة غداً: «المساحة الحقيقية ليست 1/73 بل 1/146 على مضاعفات محددة» — ضربة مدرّسة.

### لماذا «ثبّت 18 واشتق 2» (تلميح `LabSheet.md`) ليس كافياً — وللفرق بين 7% و99%
قِس عملياً على 300 بادئة عشوائية لكل نسخة: تثبيت 18 واشتقاق 2 ينجح في **7%** فقط؛ 17+3 في **38%**؛ 16+4 في **72%**؛ **الأسلوب العملي الصحيح**: ثبّت 15 (بادئة مزخرفة) وجرّب ذيلاً عشوائياً من 5 محارف — ينجح خلال ~500–14000 محاولة (أقل من جزء من الثانية حتى في Python) ويعطي مفاتيح مثل المفاتيح أعلاه. السبب البنيوي: القيدان ليسا مستقلين (المجموع وXOR مرتبطان ببتات المحارف نفسها)، ولهذا «المعادلتان بجهولين» قد لا يكون لها حل في أبجدية `[A-Z0-9]`.
**هذه نقطة تميّز حقيقية لفريقك**: عدّل تلميح `LabSheet.md` إلى «ثبّت 15 واشترِ 5 + ابحث» وأضف الجدول أعلاه — تُظهر أنك جرّبت وقيست، لا أنك نسخت.

### ملاحظات أخرى تحققت منها (مفيدة في الدفاع عن المشروع)
- `sum % 73` و`xor` **لا يرسمان مفتاحاً**: أي مفتاح صالح يعمل على أي جهاز، و`state.enc` وحده هو المرتبط بالبصمة ⇒ المفتاح ليس رخصة، بل «مفتاح تشغيل» ⇒ نقطة الضعف الأولى التي يجب أن تُذكر في ختام العرض.
- `RequiredXor` معرّف **`private const byte RequiredXor = 0x0A;`** (السطر 94) ⇒ يُولّد `ldc.i4.s 10` مضمَّناً في IL (وليس `ldsfld`) — أي أن البت `0A` قابل للتعديل هو الآخر؛ ومع ذلك اخترنا هدف `73` داخل `1F 49 D0` لأنه **أندر** في الملف، ولأن تغييره إلى `1` يجعل الشرط صادقاً **حسابياً** بدل قلب مسار المقارنة (أوضح تعليمياً وأقل كسراً لشيء آخر).
- `Optimize=false` يبقي IL خاماً (متغيرات محلية كثيرة، لا إعادة ترتيب) — جيد للعرض؛ لكن **الـ JIT ما زال يعمل** ومع `TieredCompilation` الافتراضي (On) قد تُعاد ترجمة الطريقة دارجة-1 فتتغير الأكواد الأصلية → في `Crack_Guide_x32dbg.pdf` أضفنا `DOTNET_TieredCompilation=0` قبل أي تجربة على مستوى الأوامر.
- لا يوجد في المشروع **أي فحص تكامل ذاتي** ولا Strong-Name ⇒ أي رتب يبقى سارياً (وهذا أيضاً ما ستشرحه في «كيف نحمي»: توقيع + فحص SHA256 للتجميعة + ربط النتيجة بمنطق البرنامج).

---

# (و) ما تغطّيه الـ CI وما لا تغطّيه (سبب مفاجأتك غداً)

`.github/workflows/build.yml` أخضر على `main` (آخر تشغيل 2026-09-13 ✅) ويشغّل ~50 فحصاً تشغيلياً حقيقياً — **لكن كلها على الطبقة المنطقية**: قاعدة البيانات، الخدمات، `TokenProcessor`، `ProductStateEngine`، `state.enc`، دورة حياة الفاتورة.
**لا يوجد أي فحص لـ:** بناء واجهة WPF، توليد الصفحات، معامات XAML، التخطيط RTL، التنسيق الثقافي، ولا أن الحزمة المنتجة تحوي `MizanPro.dll`.
⇒ لذلك: **كل أعطال الصور السبعة لم تكن لتُرى في CI**. أضف الفحوص الثلاثة المقترحة في §د/12، واعتمد «بروفة يدوية» كشرط تسليم — هذا بالضبط درس «Test the layer you demo».

---

## مرفق — أوامر تنظيف حالة الترخيص (ضعها في `tools/reset-license.bat`)
```
reg delete "HKCU\Software\MizanSolutions\Mizan\RuntimeState" /v ActivationBlob /f
reg delete "HKCU\Software\MizanSolutions\Mizan\RuntimeState" /v InstallDate /f
reg delete "HKCU\Software\MizanSolutions\Mizan\Settings" /f
del /f /q "%APPDATA%\MizanPro\state.enc"
```
- «وضع تجربة منتهي» للبروفة: `reg add "HKCU\Software\MizanSolutions\Mizan\RuntimeState" /v InstallDate /t REG_SZ /d 20250101 /f`
- «مُفعَّل جاهز» (للقطات): نفس الأمر لكن `/v ActivationBlob /d M1Z4N-2025P-ROM4S-T3R01`
- ملاحظة 32/64: `HKCU\Software` **غير مُحوَّل** (Redirection) لعمليات 32-بت، فـ `regedit` يعرض القيمة في مكان واحد — لا تضيّع وقتاً في `Wow6432Node` (ذلك في `HKLM\Software` فقط).
